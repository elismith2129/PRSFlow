# dependencies
node_modules/

# Next.js build output
.next/
out/

# environment variables
.env*.local
.env

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo

# Vercel
.vercel

# OS artifacts
.DS_Store
Thumbs.db

# Claude Code auto-memory
memory/

# Claude Code agent worktrees (scratch, not project code)
.claude/worktrees/
# Claude Code per-machine settings — local to whoever is working, not project code
.claude/settings.local.json

# Service account keys — never commit credentials
Google Service Account Key/
supabase/seed/*.local.sql

# Calendar-import scratch area — real client data, never commit
imports-scratch/
