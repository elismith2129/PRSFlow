# PRSFlo — Tech Stack & Roadmap

*Last updated: July 28, 2026*

---

## Stack

| Layer | Technology | Notes |
|---|---|---|
| Framework | Next.js 14 (App Router) | `"next": "^16.2.6"` in package.json; all pages are `'use client'` |
| Database | Supabase (Postgres) | Direct browser queries via anon key; no API layer |
| Storage | Supabase Storage | Private `client-ids` (ID uploads) and — since July 2, 2026 — **private `checklist-photos`** (receipts / NA / task / flag photos) buckets. Reads use short-lived signed URLs (`lib/photos.ts` + `components/shared/SignedImage.tsx`); writes store the storage path. |
| Auth | Supabase Auth + PIN login; **RLS enforced (July 2, 2026)** | PIN numpad is the primary login, email/password a fallback link; client-side `AuthGuard` on `(main)`. **RLS is now live on every table**, keyed on `user_profiles.role` via `auth.uid()` — real data security, not just UX gating. Sessions in localStorage (no SSR middleware). Roles: owner / manager / billing / asst_manager / tech / runner. |
| Hosting | Vercel | Auto-deploys from GitHub `main`; Vercel Cron for auto-demotion job |
| Backups | GitHub Actions → Google Drive (`googleapis`) | Daily cron backs up 17 Supabase tables (via REST `fetch()`) to a Shared Drive folder. Separate from Vercel; uses its own GitHub secrets. See `scripts/backup.mjs` |
| PWA | Web manifests + icons (no service worker) | Installable to home screen (`display: standalone`). Two manifests/icon sets — main app (`/manifest.json`, `start_url /`) and runner hub (`/runner-manifest.json`, `start_url /runner`, orange-accent variant). Wired via Next.js Metadata API. **No offline caching.** Icons are **THE RIBBON** (solid sea-green twisted-ribbon wave on charcoal; runner = warm amber) generated by `scripts/generate-icons.js` (`sharp` dev dep) — see spec §20 |
| Language | TypeScript | `strict: false`, `target: es5` |
| Styling | Plain CSS + CSS variables | No Tailwind (installed but unused); all inline `style={{}}` JSX. **Light/dark theming (July 13–16, 2026)** via `data-theme="light"` on `<html>` (default **light**; toggle in Nav; persisted to `localStorage['prsflo-theme']`). Light mode = a block of `[data-theme="light"]` rules in `globals.css` that redefine the CSS vars + glass/gradient overrides matched on inline-style substrings / `data-*` markers. Accent tints use `rgba(var(--accent-rgb), a)` so they follow the theme. |
| Fonts | Google Fonts via CSS `@import` | **Inter** (body/UI, weights 400–700 — the app default since July 15, 2026), Syne (headings/wordmark), DM Serif Display (display). **DM Mono** is reserved for code-like elements only (activity-log timestamps, avatar initials, invoice numbers/IDs, PIN numpad digits) via inline `fontFamily` or the `.font-mono` utility. Base `html, body` is `Inter` at 14px. |

---

## Quick Mental Model

If you're not deep in the code day-to-day, here's the 30-second version of how everything connects.

**The code lives in two places:**
- **Your laptop** — where you edit files in VS Code. Nothing here affects the live site until you push.
- **GitHub** — the cloud copy and source of truth. Every `git push` sends your local changes here.

**Vercel watches GitHub.** The moment you push to the `main` branch, Vercel automatically builds and deploys the new version. The live site at `prs-flow.vercel.app` is updated in about 60 seconds — no manual deploy step needed.

**The app talks to Supabase for all data.** Supabase is the database. It holds leads, clients, registration tokens — everything. The app running in the browser queries it directly. Supabase lives in the cloud independently of Vercel.

**The full edit loop looks like this:**

```
Edit file in VS Code
       ↓
localhost:3000 updates instantly (npm run dev is running)
       ↓
git push origin main
       ↓
Vercel detects the push → builds → deploys (~60 seconds)
       ↓
prs-flow.vercel.app is live with your changes
```

**Preview URLs** work the same way but for non-main branches. Push a branch like `chunk-4-6b` and Vercel builds a separate preview URL — useful for testing on your phone without touching the live site.

---

## Local Development Setup

When you sit down to work on PRSFlo, here's what should be running:

**VS Code**
Open the project folder (`~/Desktop/PRS/PRSFlow/prsflow`). All file editing happens here. The built-in terminal panel at the bottom is where you run commands — you can have multiple terminals open as tabs.

**Terminal 1 — the dev server**
```
npm run dev
```
This starts a local version of the app at `http://localhost:3000`. It stays running the whole time you're working. Every time you save a file, it automatically refreshes the browser — no manual reload needed.

You'll see a stream of `GET /` and `GET /crm` log lines while you use the app. That's normal — it means the server is responding to your browser. It's not stuck; it's working.

**Terminal 2 — Claude Code**
```
claude
```
The AI coding assistant. Open this in a second terminal tab so it runs alongside the dev server. You can switch between terminals by clicking the tabs in VS Code's terminal panel (bottom right of the screen).

**Browser**
- `localhost:3000` — for testing your current changes locally
- Vercel preview URLs (e.g. `prsflow-git-chunk-4-6b-....vercel.app`) — for testing on your phone or sharing with others before merging

You don't need to restart `npm run dev` when you edit files — it hot-reloads automatically. You only need to restart it if you change something in `next.config.js`, install a new package, or if it crashes.

---

## Troubleshooting Quick Reference

| Symptom | Fix |
|---|---|
| **White screen, no styles** | Check that `globals.css` is imported in `app/layout.tsx`. Try restarting `npm run dev`. |
| **Data not loading / blank lists** | Open browser DevTools → Console tab. Look for red errors. Most likely: `.env.local` is missing the Supabase API keys. |
| **Vercel deploy failed** | Open Vercel dashboard → click the failed deployment → Build Logs. Most common cause: a missing environment variable. Remember env vars must be set for **all three environments** (Production, Preview, Development) — not just Production. |
| **`git push` rejected** | Run `git pull --rebase origin main` first to pull in any changes you don't have locally, then push again. Or ask Claude Code: "resolve this push rejection." |
| **Merge conflict** | Don't panic. Ask Claude Code to resolve it — paste the conflict and it'll fix the file. |
| **Claude Code hung / waiting for permission** | Press `Esc` to cancel the pending action. For routine read/write commands you trust, choose "Yes, and don't ask again for this session" to stop being prompted repeatedly. |
| **`BEGIN`/`COMMIT` not working in Supabase SQL editor** | The SQL editor uses a separate connection per browser tab. Multi-statement transactions must be run in the same tab, in one execution. |
| **iOS Safari looks different from desktop** | Almost certainly an overflow/height issue. Add explicit `height` (not just `max-height`) and `-webkit-overflow-scrolling: touch` to the scrollable container. See the iOS Safari entry in PROJECT_LOG.md → Decisions Log. |
| **Phone can't reach `localhost:3000`** | `localhost` only works on the same machine. Use a Vercel preview URL to test on your phone, or configure the dev server to expose itself on your local network (`npm run dev -- --hostname 0.0.0.0` and use your laptop's local IP). |

---

## Key files

| Path | Purpose |
|---|---|
| `lib/tenants.ts` | **(Sep 2, 2026)** THE tenants layer — the roster (`TENANT_ROOMS`: room, tenant, rent; **code, not a table** — deal terms change by commit, the nadines precedent), the one shared-runner deal (`SHARED_RUNNER`, an object until a second tenant shares runners), the rent-stamp reads/writes (`fetchRentStamps`, `markRentSent`/`markRentPaid`/`markRentQb` + their undos — the ONLY stored facts), and `fetchSharedRunnerMonth`, which derives Mustard's month by intersecting his lockout WO's studio-time rows with billed ERS·A rows (merged intervals so a double-booked day can't double-halve an hour). **`room_id` is the `tenant_rent_months` key — never rename one once stamps exist.** Hours only; billing prices them in QuickBooks |
| `components/billing/TenantsView.tsx` | **(Sep 2, 2026)** The rent board + the Mustard month sheet. One button per row, always the next act (Mark sent → Mark paid → In QB, ↩ undoes the last). **Opens its own `studio_time_rows` channel — safe ONLY because the tenants branch unmounts `WorkOrderPopup`;** mounting this beside the popup breaks the one-channel-per-table rule |
| `lib/woActivity.ts` | **(Sep 1, 2026)** THE work-order history layer — TS diffs at the save choke points (`diffWoForSave`, `woAuditView`), the creation snapshot (`buildWoSnapshot`, stored whole — never reconstructed), the compare summarizer (`summarizeForCompare`, run identically on Original and Now so C's tint is an honest string comparison), and the append-only `wo_activity` read/write (`logWoActivity` is fire-and-forget — history must never fail a save). A write path that bypasses the logged sites (popup save, create, submit, review-lock, invoice approve) writes no history: add the call where the write lives, never a second diff engine, never a DB trigger |
| `components/calendar/WoHistoryModal.tsx` | **(Sep 1, 2026)** The ⟲ History viewer (options A+C of `wo-history-options.html`): sentence feed newest-first, actor chips (Runner amber / Office / Original teal), `after_invoice` ⚠, Original card pinned last with the Original ⇄ Now compare. Renders only — never writes |
| `components/runner/RunnerNotesChannel.tsx` | **(Sep 1, 2026)** The runner notes channel — one per studio, day-less, pure submit order, newest first. Mounted on the studio hub AND Daily Ops (admin view) so the two can never disagree. Composer above feed: RichNote, role chips that persist between sends, synchronous `lib/draft` mirror (cleared only on confirmed Send), photos upload-on-pick into `checklist-photos/runner-notes/` (paths drafted — a picked photo survives navigation). `subscribe={false}` + `reloadKey` when the host page already holds a `runner_note_posts` channel (Daily Ops does) |
| `hooks/useWoInvoicesVersion.ts` | **(Sep 1, 2026)** THE shared `work_orders`+`payment_rows` realtime signal (the `useClientsVersion` shape). Consumers: the billing hub's load, the Rail's approvals badge, the dashboard's approvals banner. Never open another channel on these tables from a surface that can share a page with those |
| `app/layout.tsx` | Root layout; font loading, global CSS. PWA metadata (Metadata API): `metadata.manifest = '/manifest.json'`, `metadata.icons` (favicon-16/32 + apple-touch-icon), `viewport.themeColor = '#0d0f14'` |
| `lib/dailyOps.ts` | Everything `/daily-ops` needs for one night in one pass — the queue (flags → absences → missing mics → attention notes, that order IS the urgency ruling) and the four-studio sweep. `opsDate(n)` defaults to YESTERDAY: this page is about last night, not today. `markReviewed`/`unmarkReviewed` write `daily_ops_reviews`; **flags are excluded on purpose** — a flag clears by acknowledgement so the flag system stays its own record. Deliberately excludes work orders (Billing), punches (HR) and live-tonight (dashboard) per spec §19 |
| `lib/punches.ts` | Single source for the punch surfaces — `PUNCH_TYPES`, `REPORT_CLASS_LABEL`, `missBand()` (green→red by miss count in a 90-day window), `composeAdpComment()` (HR-SPEC §5.5), `to24h`/`fromDbTime` for the Postgres `time` column. **When scheduling lands, add the shift-count denominator HERE** and every surface upgrades from counts to a real percentage at once |
| `components/runner/AdminReturn.tsx` | The office's way back out of `/runner/*`. Mounted once in `app/runner/layout.tsx`; renders **nothing** for the shared runner role and for unauthenticated visitors, so the installed runner PWA is unchanged. Exists because runner pages deliberately carry no nav |
| `components/dev/DeviceToggle.tsx` + `app/(main)/preview/` | **TEMPORARY build-phase tooling — delete before go-live** (Eli's ruling 2026-08-14). Owner-only floating ⧉ opens the current page inside an iPhone/iPad frame; `/preview` is the frame itself (`?path=&device=`). The rail's Runner Hub entry currently points at `/preview?path=/runner&device=phone` and **must revert to `/runner`** when these go |
| `lib/sendMail.ts` | Transactional email — **server only**. Plain `fetch` to the Resend REST API (no SDK). Holds `MAIL_FROM_DEFAULT`, the one verified sender for the domain. **Failure is always soft** — returns `{ok:false}` and logs rather than throwing, so a mail outage can never fail the feature that called it. Consumed by `/api/inquiry`; `/api/send-campaign` imports the sender constant. Env: `RESEND_API_KEY` (required), `MAIL_FROM` + `INQUIRY_ALERT_TO` (optional overrides) |
| `components/calendar/SessionCard.tsx` | **The session card — one implementation, every surface.** Renders the §10b anatomy (payload → footer band → COD strip) plus the height ladder that sheds content on stacked cards. Exports `SessionCardBody`, `sessionFillClass` (the app's only status→colour decision), `fmtCardTime`, `initials`, `CARD_FULL_H`, `cardTiers`. Consumed by the calendar grid/day/studio views and the dashboard room grid — **do not re-implement a card in a caller** |
| `app/runner/layout.tsx` | Server-component passthrough layout for the `/runner/*` subtree (runner `page.tsx` is `'use client'` and can't export metadata). Hosts the runner PWA metadata: `manifest = '/runner-manifest.json'`, `icons.apple = '/runner-apple-touch-icon.png'`, `appleWebApp` (capable, black-translucent, title 'Runner'). Overrides the root's manifest/apple-icon for runner routes only |
| `public/manifest.json` | Main-app web manifest (`name "PRSFlo"`, `start_url "/"`, `display standalone`, `theme_color #0d0f14`, icons 192/512/512-maskable) |
| `public/runner-manifest.json` | Runner-hub web manifest (`name "PRSFlo Runner"`, `short_name "Runner"`, `start_url "/runner"`, runner icons 192/512/512-maskable) |
| `scripts/generate-icons.js` | Dev script (Node + `sharp`) — holds the **wave-mark** SVG sources (main + runner; runner swaps the teal top wave for orange) and rasterizes them to the PWA PNGs + writes the SVGs into `public/`: main → favicon-16/32, apple-touch-180, icon-192, icon-512, `icon.svg` (5 PNG + 1 SVG); runner → runner-apple-touch-180, runner-icon-192/512, `runner-icon.svg` (3 PNG + 1 SVG). Re-run with `node scripts/generate-icons.js` after editing the SVGs |
| `public/icon.svg` / `public/runner-icon.svg` / `public/*icon*.png` | Generated PWA icons — the **wave design** (three teal/lime/white sine waves + drop shadow on a radial-gradient bg; runner = orange top wave). Main: `icon.svg`, `favicon-16x16`, `favicon-32x32`, `apple-touch-icon`, `icon-192`, `icon-512`. Runner: `runner-icon.svg`, `runner-apple-touch-icon`, `runner-icon-192`, `runner-icon-512`. Do not hand-edit; regenerate via `scripts/generate-icons.js` |
| `components/PRSFloIcon.tsx` | Bare wave mark (no rounded-square container — that's app-icon-only) for in-app use; `size` prop; sits on a centered teal radial glow (`rgba(93,202,165,0.12)`) sized 1.4× the icon. Used in `Nav.tsx` (38), login/reset/welcome-splash (72), runner hub (32). **Theme-aware (July 13–16, 2026):** `'use client'` + `MutationObserver` on `data-theme` — dark keeps the teal/lime SVG gradients + white `#e8eaf0`; light uses solid blue strokes (`#3b82f6` ×2 + `#93c5fd`), set directly on each path because the gradient `url(#id)`s duplicate across icon instances (so CSS `stroke` overrides were unreliable). The **PRSFlo wordmark** that pairs with it is locked to `Nav.tsx`'s exact PRS/Flo span styling (see CLAUDE.md → Locked Design Conventions) |
| `lib/billing.ts` | **The invoice lifecycle** — the whole billing hub model. Two pipelines (`billing`/`cod`) with their own tabs; `deriveStep` (0 open → 5 paid) and `deriveBucket`; the derived `awaitingPo` (from the WO's `po_number`/`no_po_needed`) and `invoiceDrift`; `nextAction` (the one button per row); transitions `approveInvoice`, `markDownloaded`, `markSent`, `markPaid`, `pullBack`, `closeInvoice`, `reopenInvoice`, `enterInvoicePipeline`; `uploadInvoiceDoc` (the drop-to-attach that also routes the WO and snapshots its total); `downloadPackage`, `signedInvoiceUrl`, `signedPackageUrl`. COD buckets are COMPUTED from payments, billing buckets are STORED — see the header comment for why the split is where it is |
| `lib/woPdf.ts` | **SERVER ONLY.** Draws the work order as a black-and-white PDF with `pdf-lib`, and `mergePackage` staples the invoice's pages (or an image) onto the end. Has a small layout cursor (`Sheet`) because pdf-lib has no concept of flow. ⚠️ **Currently a generic invoice, NOT a replica of the WO screen — rebuild pending.** ⚠️ It and the WO screen are two descriptions of one layout: row data flows through, a whole new SECTION does not |
| `lib/woTotals.ts` | `computeWoTotals` — extracted from `WorkOrderPopup` so the WO screen, the billing hub and the PDF all use ONE arithmetic. Two quirks preserved deliberately: engineer hours prefer the clock over stored `eng_hours`, and a row with no resolvable rate contributes zero |
| `lib/woValidation.ts` | `findMissingTimes` — blocks Complete WO on the admin side; in runner mode (`WorkOrderPopup mode="runner"`, since v1.11.0) the same rule renders as a live warn-only banner, so the two sides can't disagree about what "done" means. **(Sep 3, 2026)** `woNeedsTimes(status)` is the ONE lockout exemption predicate (extend it, never add a second flag); `confirmStartProblem` blocks SAVING a Confirmed WO whose dated days lack From/To (tentative saves freely); `findMissingTimes(rows, asOf)` skips days that haven't happened; `problemsDetail` caps every banner at four rows + "+ N more" |
| `app/api/wo-package/route.ts` | `GET ?id=…[&wo=1]` → ONE PDF: the work order plus the invoice's pages. Verifies the caller's bearer token AND role before the service-role client reads anything. Stores the exact bytes it serves at `work_orders.invoice_package_path` so the package window can show the artifact, not a re-render. `wo=1` returns the work order alone (the WO screen's Save & download) |
| `app/(main)/billing/page.tsx` | The billing hub. Replaces `/wo-hub` and the Dropbox folders. Pipeline toggle, locked 8-column grid + header strip, three derived lights per row, drag-and-drop invoice attach, pinned Upcoming sub-view, package window (`As sent` / Work order / Invoice), ⋯ overflow (open invoice, re-download, pull back, close). **Do not edit by cutting between two matched strings — see PROJECT_LOG, Aug 11–12** |
| `docs/design-refs/billing-hub-v2.html` | The approved billing-hub design. `billing-hub-final.html` is the STALE nine-tab v1 and carries a banner saying so — do not port from it |
| `app/(main)/` | Route group for internal nav-bearing pages (gated by `AuthGuard`) |
| `app/(auth)/login/page.tsx` | Styled login — `signInWithPassword`; "Forgot password?" → `resetPasswordForEmail` |
| `app/(auth)/reset-password/page.tsx` | New-password page — `updateUser({ password })` |
| `components/auth/AuthGuard.tsx` | Client-side route guard wrapping `app/(main)/layout.tsx`; redirects unauthenticated users to `/login` (localStorage sessions; no SSR middleware) |
| `hooks/useUserProfile.ts` | `{ profile, loading }` — resolves the logged-in user's `user_profiles` row by session email. Single source of profile-fetch logic |
| `hooks/useIsMobile.ts` | `useIsMobile(breakpoint = 768)` — `matchMedia` hook, desktop-first (`false` on first render, flips on mount). The single mobile breakpoint; gates every responsive layout (dashboard, Nav, CRM, calendar, BookingForm, WorkOrderPopup, LocationStrip) |
| `components/carved/index.tsx` | **Carved design-system primitives** (July 30, 2026). Class-driven, zero inline styles, consumes `--c-` tokens only. Also exports `toCarvedStatus()` / `statusFillClass()` — the app-status → six-slot status mapping. Note it deliberately differs from the legacy `StatusBadge` map: here `uncontacted` is harbor (a live lead state) and `open`/`tech` are driftglass (inert). |
| `components/layout/Wordmark.tsx` | **The PRSFlo wordmark — single source of truth, site-wide.** Rendered by nav, login, reset-password, runner hub, SOP gate; only `size` may differ. Do not reintroduce inline `PRS`/`Flo` spans. Replaces the old copy-these-values rule in `CLAUDE.md`. |
| `app/(main)/dev-style/page.tsx` | Carved style guide — every primitive and state, both themes via a local `data-theme` toggle (doesn't write `localStorage`). Auth-gated, **no nav link** — type the path. Approve design changes here before touching real surfaces. |
| `components/layout/NavGate.tsx` | Wraps `<Nav>`; hides the nav on first paint during the fresh-login welcome splash (`showWelcome` flag) and reveals it on the `welcomeDone` event (3s fallback). `layout.tsx` renders this instead of `<Nav>` directly |
| `lib/financials.ts` | **The only place the revenue archive and live money are joined.** Live studio-time/rentals come back as daily rows; `financial_history` comes back pre-aggregated by `financial_monthly()` — **never select raw rows from it, PostgREST truncates at 1,000 silently.** `buildSeries()` folds both into one month map and handles the partial-month day-cap comparison. `Compare` is `'prev'` (rolling) or a year string (pinned baseline). |
| `components/billing/FinancialsView.tsx` | The revenue chart. Hand-drawn SVG, no chart library. Two modes: **Timeline** (one continuous line, brush zoom) and **Years** (Jan–Dec overlay, line per year, single-hue ramp — hues would imply categories, years are ordered). **No `<table>` anywhere** — `.c-table-row` is `display: grid` and destroys table layout. |
| `scripts/extract-financial-history.py` | Workbook → one CSV per year. Holds ALL the judgement about the spreadsheet's shape (which columns are real, which are roll-ups, Track's drifting headers) and reconciles day-by-day against the sheet's own GROUP TOTAL **columns** — not the `TOTAL <year>` rows, which are stale in 2024–2026. |
| `scripts/import-financial-history.mjs` | CSVs → `financial_history`. Deliberately dumb: the columns already match the table. Dry-run by default, `--commit` writes, re-runnable (upserts on `source_key,category`). Splits on CRLF — Python's `csv.writer` emits it. |
| `supabase/user_profiles.sql` | Migration: `user_profiles` table + seed (run manually in Supabase SQL editor) |
| `supabase/dashboard-tasks-assignment.sql` | Migration: `dashboard_tasks.assigned_to` / `assigned_by` uuid FK → `user_profiles.id` |
| `app/crm/page.tsx` | CRM — canonical pattern reference for all new pages |
| `app/clients/page.tsx` | Clients list + detail panel |
| `app/register/[token]/page.tsx` | Public registration form (no nav) |
| `app/api/cron/auto-demote/route.ts` | Vercel Cron endpoint — demotes Hot/Warm leads daily at 9am |
| `lib/supabase.ts` | Supabase client + all entity types (`Lead`, `Client`, etc.) |
| `lib/createWorkOrder.ts` | Canonical work-order creator (June 30, 2026). `createWorkOrderForBooking(booking)` inserts one `work_orders` row + seeds `studio_time_rows` (via the shared `seedStudioTimeRows` helper) + `equipment_condition_rows`, idempotent via `upsert(onConflict:'booking_id', ignoreDuplicates)` (seeds only on a fresh insert). Exports `bookingShouldHaveWorkOrder(booking)` — Recording/Filming/Event-Playback get WOs; Tech/Tour/Open Hours/cancelled excluded (gated on `status`). Called once at booking-save from `calendar/page.tsx handleSave`. **The only code path that creates a WO.** Requires the `UNIQUE (booking_id)` constraint. **(July 21, 2026)** now also sets `bookings.work_order_id` (the new link direction) after create/adopt. **(July 28, 2026 session 2)** the whole create is now ONE call to the `create_work_order_atomic` Postgres RPC (all-or-nothing; payloads still built in TS via `buildSeedRowPayloads`); rows seed `eng_name`/`eng_role` from the booking's engineer/assistant. |
| `lib/seedStudioTimeRows.ts` | **(July 21, 2026)** Shared, **append-only** `studio_time_rows` generator — one row per date with correct day/hourly columns; skips dates already present (never overwrites). Called by both `createWorkOrderForBooking` (initial seed) and the WO **Seed panel** (bulk-add a date range). Also exports `dateRange`, `toStudioLetter`. See docs/WO-SPEC.md §6. **(July 28, 2026 session 2)** also exports `buildSeedRowPayloads(params)` — same single-source row builder WITHOUT the insert, used to hand prebuilt rows to `create_work_order_atomic` (the RPC injects `work_order_id`); optional `engName`/`engRole` seed the per-row staff sub-row. |
| `components/shared/ClientPanel.tsx` | **(July 21, 2026)** Self-contained client identity/contact block used at the top of the Work Order — SRS toggle, COD/Label-Billing toggle, client/A&R/artist search + autofill, inline add-contact, contact-update prompt, view-full-profile. `value: ClientPanelValue` / `onChange(patch)` interface (emits only the client-subset of session fields); owns its own search/roster/contact state. Faithful port of BookingForm's right column (BookingForm still has its own copy until it's deleted, Step 8). |
| `lib/myday.ts` | **(Aug 10, 2026)** THE My Day operational duties layer. Duty fetch/complete (`fetchDuties`, `completeDuty`, `uncompleteDuty`, `setDutyCaptured`), backlog (`computeBacklog`), sticky monthly escalation (`computeOverdueSince`, `STICKY_CADENCES`), the five COMPUTED queues (`fetchNeedsWoQueue`, `fetchBalancesQueue`, `fetchHoldsQueue`, `fetchBookedQueue`, `fetchOpenHoursQueue`), step ticks (`fetchQueueSteps`/`setQueueStep`), scratchpad (`fetchNotes`/`saveNotes`), the 14-day grid (`fetchStaffGrid`), the manager's read-only billing peek (`fetchBillingBrief`), and `composeBriefing` — the Flo briefing, **template sentences over real numbers, no AI**. Key distinctions: `isDutyDueOn` (compliance — drives grid/backlog/briefing) vs `isDutyShownOn` (UI); `dutyExistedOn` bounds every retrospective judgement by `created_at` so day one isn't a wall of red. |
| `lib/woTotals.ts` | **(Aug 10, 2026)** THE canonical work-order money math — `computeWoTotals({studioRows, rentalRows, paymentRows, fallbackEngRate})` → studio/engineer/rentals/grand/paid/balance, plus `engChargeForRow`. Extracted from `WorkOrderPopup`'s derived-totals block so My Day's balances queue reuses it instead of re-deriving. Structurally typed: the popup passes form state (money as `"$1,450"` strings), a DB read passes numerics. Preserves two deliberate quirks — engineer hours prefer the CLOCK over stored `eng_hours`, and a row with no resolvable rate contributes zero. |
| `lib/woValidation.ts` | **(Aug 10, 2026)** What a WO must satisfy before it can be COMPLETED — `findMissingTimes(rows)` + `missingTimesMessage()`. Studio rows need From/To; any active staff line needs its own. **Blocks Complete WO** on the admin popup; **warns only** on the runner WO page, whose sole action is Save (a session in progress legitimately has no end time). One file so the two surfaces cannot disagree about what "done" requires. |
| `lib/time.ts` | **(July 28, 2026)** THE canonical session time/date math — `timeToMins`, `calcHours` (NaN-guarded; unparseable times → null, never phantom hours), `calcCharge`, `dateRange`, `isNextDay`, `toStudioLetter`. Never re-define these locally; import from here. |
| `lib/format.ts` | **(July 28, 2026)** Canonical formatters: `formatCurrency`, `stripCurrency`, `fmtTimestamp` ("Jul 21 · 08:30 PM"), `fmtClock`. Page-specific chip formats stay local by design. |
| `lib/errlog.ts` + `app/api/log-error/route.ts` | **(July 28, 2026)** First-party error reporting: `logAppError()` fire-and-forgets to the rate-limited service-role route → `app_errors` table. Used by error boundaries, window listeners, and `dbResult`. |
| `lib/db.ts` | **(July 28, 2026)** `dbResult(label, error)` — the shared save-failure check: red "NOT saved" toast + app_errors report. Wrap every important Supabase write with it. Adopted across CRM's critical writes. |
| `components/ui/Toaster.tsx` | **(July 28, 2026)** Global toast system (`toast(msg, kind)` via CustomEvent), mounted in the root layout. |
| `components/ErrorReporter.tsx` | **(July 28, 2026)** Window `error` + `unhandledrejection` listeners → `logAppError`. Mounted in root layout. |
| `app/error.tsx` / `app/global-error.tsx` | **(July 28, 2026)** Route + root error boundaries — styled recovery screens that auto-report; no more white pages. |
| `components/admin/ErrorsSection.tsx` | **(July 28, 2026)** Admin → Errors tab (owner/manager only, realtime): app_errors viewer with expandable stacks + load-more. |
| `docs/AUDIT-2026-07.md` | **(July 28, 2026)** Full architecture audit — verdict (no rewrite; phased rework), measured risks, phase plan. Phases 0–1 shipped July 28. |
| `hooks/useClientsVersion.ts` | **(July 28, 2026 session 3)** THE shared `clients` realtime signal. One module-level `clients-shared` channel, opened on first subscriber / removed after the last, exposing a version counter components use as a re-fetch trigger (`useEffect(() => { load() }, [load, clientsVersion])`). Same shape as `WebInquiryProvider`'s `leadsVersion`, minus the context. Consumers: `RegistrationBanner`, `RegistrationsView`, `ClientsPageInner`, Nav reg badge. **Never open another `clients` channel — use this.** |
| `lib/propagateClientRename.ts` | **(July 28, 2026 session 3)** `propagateClientRename(after, changed)` / `propagateContactRename(after, changed)` — reconcile a rename onto the records that denormalize it: bookings (`label`/`client_name`/`ordered_by`) and leads (`label`/`fname`/`lname`), matched by `client_id` or `anr_contact_id`. **Name fields only** — artist is per-session for a label, and contact details record who was reachable at booking time. WOs need no pass (they read the client through the booking). Called from `ClientProfile.saveClient` / `saveContact`; `dbResult`-checked. |
| `components/shared/StaffPicker.tsx` | **(July 28, 2026, v1.2.0)** Lead staffing control — Eng / Asst / **No Staff** + an optional person. Shared by the CRM lead detail and the new-lead modal so the two can't drift. `useStaffPools()` reads the `engineers` roster (module-level cache; `role='Both'` lands in both pools) and feeds a `<datalist>` that still accepts free text. Emits `{role, name}` together — switching role clears the name **on purpose** (the previous person came from the other pool), so callers must never persist the two independently. |
| `components/crm/RegistrationsView.tsx` | **(July 28, 2026 session 3)** CRM REGISTRATIONS tab — every client with a `registered_at`, newest first (Name · Email · Phone · City/State · Submitted · ID badge). Search + submitted-date range, 25/page pager, row opens `RegViewModal`. Filtering/paging client-side by design. |
| `components/clients/RegistrationBanner.tsx` | **(July 28, 2026 session 3)** Pending-registration banner + review modal, extracted from `ClientsPageInner` and mounted at CRM page level so it's visible on every CRM tab. `onNavigate(clientId)` hands the confirmed profile back to the host page. |
| `components/shared/RegViewModal.tsx` | Registration record modal (full fields, ID image/PDF with lightbox, Export PDF). **(July 28, 2026 session 3)** address fields grouped into one block with a **Copy Address** button; exports `buildMailingBlock()` (`Name / Street / Street 2 / City, ST ZIP`, blanks dropped). |
| `docs/WO-SPEC.md` | **(July 21, 2026)** Master plan + rationale for the unified Work Order (Booking = WO) rebuild. Read before touching WO/booking/calendar code. Field map, projection model (one WO → many cards), Seed design, WO numbers, notes taxonomy, 10-step build order, resolved decisions. |
| `lib/settings.ts` | Timer constants (COOL_DOWN_DAYS, TOUCH_INTERVAL_DAYS) |
| `lib/terms.ts` | T&Cs content as structured array — update here without touching form |
| `styles/globals.css` | CSS variable definitions + Google Fonts import; `@media (max-width: 768px)` rules (`overflow-x: hidden` guard + `.page-main` gutter `16px 12px !important`) and the `welcomeFadeIn` keyframe |
| `components/layout/Nav.tsx` | App nav (only inside `(main)`). Desktop: logo (links to `/`) + tabs + Sign Out. Mobile (≤768px): logo + 44px `≡` hamburger → full-width dropdown (nav items + Sign Out). The live clock and the experimental "⚡ USF" button were both removed from the nav |
| `components/shared/StudioSelect.tsx` | Single flat dropdown for "Venue · Studio" selection; used across CRM and calendar |
| `components/shared/TimeInput.tsx` | Smart-parse text `<input>` with auto-format on blur. Accepts `10a`→`10:00 AM`, `930p`→`9:30 PM`, `1430`→`2:30 PM`, bare `8`→`8:00 AM`. Enter commits. Used in booking form and WO Studio Time From/To cells. (Was briefly a 30-min `<select>` June 5–10, 2026 — reverted; select was harder to use on mobile.) |
| `components/shared/` | Reusable pickers: `ContactPicker`, `ArtistPicker`, `StudioSelect`, `TimeInput` |
| `lib/studios.ts` | `STUDIO_LOCATIONS` array + `parseLocation()` / `combineLocation()` for the "Venue · Studio" string format |
| `lib/roster.ts` | Label artist array helpers: `addArtistToLabel`, `removeArtistFromLabel`, `getArtistsForLabel` |
| `components/shared/RegViewModal.tsx` | Registration record view modal (used by CRM lead card + Clients profile). Fetches client data; the ID photo signed URL comes from `/api/client-id-photo` (July 15, 2026 — the private `client-ids` bucket). Card carries `data-modal-gradient` (light-mode gradient). The Government-issued ID renders **inline with an in-app lightbox** (z 10004): images → thumbnail + click-to-enlarge `<img>`; PDFs → inline `<iframe>` + Expand; `isImagePath` + an `onError` fallback so extensionless images and real PDFs both display. Export PDF button opens the print route. |
| `components/calendar/WorkOrderPopup.tsx` | Full Work Order modal — the emerging single source of truth (Booking = WO rebuild). **(July 21, 2026)** Rebuilt top section: WO number in header, colored **status bar** (`SESSION_STATUS_COLORS`), left panel card (Session Type + Invoice # + PO # + Food Budget + internal-only **Booking Notes**), right = `<ClientPanel>`; the old flat studio/rate/date/time/engineer header was removed (those live only in the Studio Time table). Collapsible **Seed panel** above the table (`seedStudioTimeRows`). Persists the session-level fields (`session_status`, `session_type`, `client_id`, `is_srs`, `cod_method`, `anr_*`, `booking_notes`, `wo_number`) to `work_orders` and mirrors to the booking. **Projection (Step 5a):** on save, syncs `start_date`/`end_date`/`from_time`/`to_time` **+ `studio`** (bare-letter→full-room-label via `roomLabelForVenue`) + `work_order_id` onto the booking card (studio *is* synced now, unlike before). Studio Time table + equipment/rentals/payments/notes/signature unchanged. Mobile keeps its read-only SESSION INFO card (new top is desktop-only). `initWO` adopt-first (June 30). |
| `components/unified/UnifiedSessionForm.tsx` | **Experimental** single-surface session form (client card + Work Order in one natural-flow modal); renders `WorkOrderPopup` inline. Owner-gated via a temp "⚡ USF" Nav button (`localStorage.userRole === 'owner'`). Parallel to — not a replacement for — the production `BookingForm` + portaled `WorkOrderPopup` flow. |
| `app/register/view/[clientId]/page.tsx` | Print route for registration PDF. Server component; generates signed ID photo URL server-side. `PrintTrigger` fires `window.print()` after 800ms. |
| `app/(main)/sop/page.tsx` | SOP / Training tab — full-viewport iframe pointing to `/sop.html` |
| `app/(main)/daily-ops-log/page.tsx` | Daily Ops Log route — wraps `DailyOpsLogSection`; also embedded as Admin sidebar tab |
| `app/runner/[studio]/wo/[id]/page.tsx` | Runner WO form — studio time table, equipment condition, expenses, eng hours, receipt OCR, canvas signature pad (COD-only), payment rows (editable), Save/Finish footer |
| `app/(main)/wo-hub/page.tsx` | WO Hub — all work orders list, filterable by studio/date/status; linked from nav |
| `components/admin/DailyOpsLogSection.tsx` | Date-based historical ops log per studio; studio tabs, date list with status dots, day modal (WO cards + 5 checklist rows with Runner/Admin checkboxes); click WO card → WorkOrderPopup |
| `components/admin/MicInventorySection.tsx` | Admin "Mic Inventory" sidebar tab. Consolidated cross-studio mic view: full-width missing-mic banner, horizontal studio tabs (Paramount/Ameraycan/Encore/Track/Floating Gear), status-colored table (Here/Room/Missing), per-tab Show History (last 7 nights). **Admin editing (June 24):** per-row `✎` turns the row's cells into in-place inputs (Status dropdown, per-studio Room dropdown, Qty, "Initials" field) → upserts `mic_checkins` with `source:'admin'`/`amended_by`; a changed Qty upserts `mic_inventory_quantities`. **Manage Mics modal:** Master Mic List (search, inline edit name/studio/category/qty, deactivate/reactivate) + Add Mic; mic catalog writes to `mics`, qty to `mic_inventory_quantities`. Reads `mics`/`mic_checkins`/`mic_inventory_quantities`/`mic_inventory_submissions`, reduced client-side to latest-per-(studio,mic). |
| `app/runner/[studio]/mics/page.tsx` | **THE SHEET (Aug 25, 2026, Option A).** Runner mic inventory as a birdseye grid: per-studio tabs (home first — PRS/ARS/ERS/TRK/Floating/Odds, progress counts), tap = HERE, tap-again → Room/Missing/Clear popover (fixed-position), pinned search, missing-streak alert, display-only last-night refs (NEVER pre-filled — theft rule), odds qty steppers. Save (progress) + Submit (initials auto-filled from profile) write `mic_checkins`/`mic_inventory_quantities`/`mic_inventory_submissions` + `daily_ops_submissions` — unchanged from v1. All rendering inline JSX: components defined inside the page component caused the old jump-to-top remounts |
| `lib/draft.ts` | **(Aug 24, 2026)** Runner input never dies: `draftKey`/`readDraft`/`writeDraft`/`clearDraft` — batch pages (stock, petty cash, mics) mirror typed-but-unsaved input to localStorage, restore on return, clear ONLY on successful save. Use on every new runner batch surface. **The office equivalent is a TABLE, not this** — see `myday_note_drafts`: managers move between a desk and an iPad, so their draft has to leave the device |
| `lib/billing.ts` → `deriveStep` / `nextAction` / `awaitingPo` | **(v1.19.1, Aug 31, 2026)** The billing hub's whole rung ladder. `awaitingPo` is DERIVED (`!isCod && step >= 2 && !poNumber && !noPoNeeded`) — never stored — so typing the PO on the work order clears it with no second act. **It warns from step 2 but only DISABLES from step 3**: the PO blocks Download/Mark sent, not Approve (ruling reversed 2026-08-31 — approval is an internal statement about the numbers). The hub decides what it disables; see `blocked` in `app/(main)/billing/page.tsx` |
| `components/shared/RichNote.tsx` | **(Aug 26, 2026)** The app's ONLY rich-text surface — bold, bullets, tab/shift-tab indent, nothing else. No editor dependency: contentEditable + `execCommand`. Exports `RichNoteEditor`, `RichNoteView`, `sanitizeNote`, `noteText`, `noteIsEmpty`. **Everything renders through the whitelist** (b/strong/ul/ol/li/div/p/br, zero attributes) at edit-sync AND display; paste forced to plain text; legacy plain-text notes (no `<`) render as pre-wrap, so no data migration was ever needed. The editor never rewrites `innerHTML` while focused (that throws the caret to the start). Used by runner shift notes, My Day notes, Daily Ops (read-only) |
| `lib/time.ts` → `opsToday()` | **(Aug 28, 2026)** The operational day, 8:50 AM LA (alias of `shiftLogDate()`). **Every runner surface keys on this**; `getLocalToday()` is calendar semantics only (booking dates on admin surfaces). Same boundary as the shift-note seal on purpose — if it moves, `shiftLogDate()` and the `shift_note_docs` / `shift_log_entries` RLS policies move together, because the interval is hard-coded in the SQL. Punches deliberately stay on the CALENDAR day (payroll counts midnights) |
| `scripts/importCalendar2026.mjs` | **(Aug 26, 2026)** The 2026 WordPress calendar import. Snapshots and then **DELETES every `bookings` row** before inserting (one-time approval — the table was test data at the time). Stamps `bookings.imported_at`; see the migration header `20260826150000` for how that flag is read (provenance, not state) |
| `app/runner/[studio]/stock/page.tsx` | **Stock v3 (Aug 24–25, 2026).** PRS Stock / Office landing (office Wednesdays-only + DUE TODAY pulse), collapsible category groups, one-line rows, tap-to-expand notes + per-date history (`stock_checks`; qty TEXT), `stock_items` current-state mirror, draft persistence, dirty-guarded realtime. Unsaved add-item rows stay editable until Save and can be × discarded; saved rows have no delete (office manages the list) |
| `components/dashboard/LocationStrip.tsx` | 4-studio dashboard strip; drawer with Yesterday/Today sessions + daily ops rows; real-time subscriptions |
| `lib/checklist-items.ts` | Per-studio opening/closing checklist items; `CHECKLISTS[studio][type]`, `getChecklistSections()`, `flattenSections()` |
| `public/sop.html` | Self-contained interactive training guide. **Four sections behind a left sidebar nav — CRM · Clients · Tasks · Flags** (collapses to a horizontal pill row ≤768px; lime active state); status/step content is JS-data-driven (`STATUS_DATA`, `CRM_STEPS`, `CLIENT_STEPS`). Tasks + Flags added July 8, 2026. Replace file to update content; no code change needed. |
| `components/ui/StatusBadge.tsx` | Unified status pill (5px dot + uppercase label, translucent tinted bg/border). Canonical renderer for record statuses. Color map: gray (uncontacted/open/dead), orange (hot/pending/tentative), yellow (warm), blue (cold), teal (booked/completed/resolved/confirmed), lime (needs_action/in_progress), red (cancelled); unknown→gray. |
| `components/ui/SectionHeader.tsx` | Unified section heading: uppercase DM Mono 11px title (`#9ca3af`) + optional count pill (`lime`/`orange`/`teal`) + optional right-aligned action link (`{label, href?\|onClick?}`), `margin-bottom: 12`. For section/panel headings only — not page titles, tabs, or field-group labels. |
| `lib/tasks.ts` | Shared dashboard-task helpers — single source of truth for the task tab/roster logic, imported by both the dashboard Tasks panel and `/tasks` (`TAB_DEFS`, `ASSIGN_OPTIONS`, `resolveAssignTo`, `nameForId`, `visibleTabsForRole`, `idsForTab`, `fetchTasks`, `fetchCompletedTasks`, `fmtTaskTime`, `uploadTaskPhoto`) |
| `lib/tags.ts` | `STARTER_TAGS` constant — the shared quick-chip list (Label A&R, Artist Manager, Producer, Engineer, VO, Leasing, Location Scout/Filming, Content Creator, Brand Partnership, Event/Listening) used by the tag UIs in lead detail, new lead modal, and client profile |
| `app/api/send-campaign/route.ts` | Email campaign send route. Requires `RESEND_API_KEY`. POST: segments recipients from the request body, personalizes body with `[First Name]` substitution, sends via Resend REST API from **`Paramount Recording Studios <info@paramountrecording.com>`** (changed from `studio@` July 20–21, `2c199bd`), logs the campaign to `email_campaigns` table via service-role client. Returns `503` when `RESEND_API_KEY` is absent. |
| `app/(main)/tasks/page.tsx` | Full task list (dashboard "show all tasks →" target; not in nav). 6 per-user tabs + role visibility, Active + Completed (collapsible) sections, search box, replicated task-detail modal |
| `scripts/backup.mjs` | Daily backup — Node ESM (no Next/React). Reads 17 Supabase tables via REST `fetch()` (not `@supabase/supabase-js` — avoids the Node WebSocket crash), paginates by `offset`, excludes soft-deleted rows, uploads `prsflow-backup-YYYY-MM-DD.json` to Drive via `googleapis` (`GOOGLE_SERVICE_ACCOUNT_JSON`). Run locally with `node --env-file=.env.local scripts/backup.mjs` |
| `.github/workflows/daily-backup.yml` | GitHub Actions: daily backup. Cron `0 8 * * *` (8am UTC) + `workflow_dispatch`, Node 24, `checkout → npm ci → node scripts/backup.mjs`. Secrets: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `GOOGLE_SERVICE_ACCOUNT_JSON` |
| `app/inquiry/page.tsx` | Public, unauthenticated lead-capture form (outside `(main)`, no AuthGuard). **POSTs to `/api/inquiry`** (service-role route, 3/min per IP) — no longer a direct anon insert; in-place thank-you; studio-photo bg (`public/inquiry-bg.jpg`) + frosted card + `public/paramount-logo.png` |
| `components/notifications/WebInquiryProvider.tsx` | Global provider (mounted in `(main)/layout.tsx` inside AuthGuard) owning ONE `leads` realtime channel; `useWebInquiries()` exposes `{ count, isUnacked, toasts, dismissToast, leadsVersion }`. Drives the Needs-Action pulse, tab-title badge, and toasts; `leadsVersion` is the shared re-fetch signal for list views. **Reference implementation for the real-time standing rule.** |
| `components/notifications/WebInquiryToaster.tsx` | Renders the transient site-wide Web Inquiry toasts from the provider's `toasts` |
| `supabase/realtime-publication.sql` | Batch migration adding all list/detail tables to `supabase_realtime` + `REPLICA IDENTITY FULL` (run once, manual, before the subscribing code deploys) |
| `supabase/leads-realtime.sql` | Adds `leads` to `supabase_realtime` + `REPLICA IDENTITY FULL` (Web Inquiry realtime; run once, manual) |
| `supabase/user_profiles-initials.sql` | Adds `user_profiles.initials` + seeds it by `display_name ILIKE` (run once, manual) |
| `scripts/importCalendar.mjs` | One-off (run-once) calendar import script; uses the service-role key. Kept for reference |
| `scripts/set-staff-passwords.mjs` | One-time (re-runnable) script — sets a random 32-char Supabase Auth password on every staff member with an `auth_user_id` (`supabase.auth.admin.updateUserById`) and stores it in `staff_pins.supabase_password`, so the PIN route can sign in via `signInWithPassword`. Run after the `…_staff_pins_supabase_password` migration: `node --env-file=.env.local scripts/set-staff-passwords.mjs`. Skips rows with no `auth_user_id` (the shared runner). |
| `schema.sql` | Full database schema — run in Supabase SQL editor to recreate. **Note:** stale/incomplete (predates most tables + RLS); the migrations under `supabase/migrations/` are the source of truth for the current schema. |
| `supabase/migrations/*.sql` | Versioned SQL migrations, run manually in the Supabase SQL editor (Claude has no DDL access). July 2, 2026 security set: `…_rls_security_hardening`, `…_staff_pins`, `…_pin_auth`, `…_sop_acknowledged`, `…_dashboard_tasks_is_private`, `…_dashboard_tasks_visibility`, `…_tech_read_bookings_clients`, `…_api_rate_limits`, `…_drop_leads_anon_insert` (run last, after deploy). July 8, 2026: `…_staff_pins_supabase_password` (adds `staff_pins.supabase_password` + DROP/CREATEs `verify_staff_pin` to also return it — enables the faster `signInWithPassword` PIN login; pair with `scripts/set-staff-passwords.mjs`). July 9–13, 2026 (**both still to run**): `20260709120000_flags_photo_url.sql` (adds `flags.photo_url` for flag photo attachments) and `20260713120000_app_feedback_temporary.sql` (creates the temporary `app_feedback` board table + RLS + realtime). **July 21, 2026 (WO rebuild — all run):** `20260721120000_wo_number_and_booking_wo_link.sql` (`work_orders.wo_number` from `wo_number_seq` + `bookings.work_order_id`), `20260721130000_work_orders_session_fields.sql` (session-level fields on `work_orders`, backfilled from booking), `20260721140000_work_orders_booking_notes.sql` (`work_orders.booking_notes`). Plus an ad-hoc backfill linking existing `bookings.work_order_id`. **July 28, 2026 (all run):** `20260728120000_bookings_wo_number.sql` (WO number on cards), `20260728130000_app_errors.sql` (error sink), `20260728140000_app_errors_realtime.sql`, `20260728150000_studio_time_rows_location.sql` (per-row venue), `20260728160000_studio_time_rows_eng_name.sql` (per-row engineer). **July 28, 2026 session 2 (both ⚠️ STILL TO RUN — run BEFORE pushing the code):** `20260728170000_studio_time_rows_eng_role.sql` (`eng_role` `'engineer'|'assistant'` — the 1ST/2ND staff toggle) and `20260728180000_wo_atomic_rpcs.sql` (**`create_work_order_atomic`** + **`save_work_order_atomic`** — all-or-nothing WO create/save+projection; generic jsonb appliers in a new non-exposed `app_private` schema; SECURITY INVOKER so RLS applies; values still computed in TS — no second copy of seeding logic). |
| `lib/rateLimit.ts` | `checkRateLimit(admin, bucket, ip, limit, windowMs)` — fixed-window per-IP limiter backed by `api_rate_limits` (service-role only). Used by `/api/inquiry` (3/min) + `/api/ocr-receipt` (10/min). |
| `lib/photos.ts` | Private-bucket helpers for `checklist-photos`: `signedPhotoUrl`/`signedPhotoUrls` (1-hr signed reads) + `toStoragePath` (strips the legacy `/checklist-photos/` prefix so old full-URL rows still resolve). |
| `components/shared/SignedImage.tsx` | Renders a `checklist-photos` image from its stored path via a signed URL; optional `link` wraps it in a new-tab anchor. Replaces raw `<img src={publicUrl}>`. |
| `components/SopGate.tsx` | First-login SOP gate — full-screen, non-dismissable modal mounted in `(main)/layout.tsx`; shows until `profile.sop_acknowledged`, calls `acknowledge_sop()` RPC → navigates to `/sop`. |
| `app/api/register/route.ts` | Service-role registration route (GET validates token + returns prefill; POST submit / use-existing / create-new incl. email-conflict). Replaces the old browser anon writes. |
| `app/api/inquiry/route.ts` | Service-role public inquiry route — 3/min per IP; inserts the Web Inquiry lead with server-forced `status`/`source`. The only insert path once `leads_ins_anon` is dropped. |
| `app/api/auth/pin/route.ts` | PIN login route (service role) — DB per-IP lockout, `verify_staff_pin` RPC (bcrypt), then `signInWithPassword` on a **dedicated anon-key client** → returns the session `access_token`/`refresh_token` for the browser's `setSession` (single round-trip; replaced the July 2 magic-link `generateLink`→`verifyOtp` flow on July 8, 2026). `403 no_account` when the profile lacks an `auth_user_id` or a provisioned `supabase_password`. |
| `app/api/client-id-photo/route.ts` | **(July 15, 2026)** Service-role route that signs a registration ID file in the private `client-ids` bucket. `GET ?storagePath=…`, Node runtime + `force-dynamic`, `createSignedUrl` (60-min TTL), normalizes legacy full-URL values to a bare storage path, returns `{ signedUrl }`. Used by `RegViewModal` (the browser anon key can't sign these, and `lib/photos.ts` is scoped to the `checklist-photos` bucket). |
| `app/(main)/feedback/page.tsx` | **⏳ TEMPORARY (July 13, 2026) — remove when rollout ends.** Staff feedback board (bug / suggestion / question) backed by the `app_feedback` table (RLS + realtime). Reached via a lime **Feedback** nav item in `Nav.tsx` (also marked temporary). |

---

## Environment variables

All must be set for **all three Vercel environments** (Production, Preview, Development):

| Variable | Used by |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase client (browser) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase client (browser) |
| `SUPABASE_SERVICE_ROLE_KEY` | Cron endpoint (server-side) |
| `CRON_SECRET` | Cron endpoint auth header |
| `NEXT_PUBLIC_BASE_URL` | Registration link URL base (e.g. `https://prs-flow.vercel.app`). Falls back to `window.location.origin` if unset, which produces `localhost` URLs in dev — fine for testing but wrong for links sent to clients. Set this in Production and Preview. |
| `ANTHROPIC_API_KEY` | `/api/ocr-receipt` receipt OCR (server-side) |
| `RESEND_API_KEY` | `/api/send-campaign` email sending via Resend. **Not yet set** — domain `paramountrecording.com` is pending DNS verification. Once Resend shows "Verified", generate an API key and add it here. Until then, `/api/send-campaign` returns `503`. |

**GitHub Actions secrets** (separate from Vercel — repo Settings → Secrets and variables → Actions), used only by `.github/workflows/daily-backup.yml`:

| Secret | Used by |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `scripts/backup.mjs` (Supabase REST) |
| `SUPABASE_SERVICE_ROLE_KEY` | `scripts/backup.mjs` (Supabase REST). **Since July 2, 2026** the backup uses the service-role key — with RLS on, the anon key can no longer read the tables. **This secret must be added to GitHub Actions.** |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | `scripts/backup.mjs` (Drive upload — full service-account JSON, parsed at runtime). The target Drive must be a Shared Drive with the service account added as a member (service accounts have no personal Drive quota). |

---

## Design system tokens

Defined in `styles/globals.css`. **Dark theme (default token values):**

```
--bg           #0d0f14        Page background
--surface                     Slightly lighter than bg; input/card backgrounds
--surface2                    Active nav, panel backgrounds, checkbox containers
--border                      Subtle dividers and input borders
--text                        Primary text
--text2                       Secondary text
--text3                       Muted/tertiary text, section labels
--accent       #c8f04e        Yellow-green; buttons, highlights, active states
--accent-rgb   200, 240, 78   RGB triple of --accent; use rgba(var(--accent-rgb), a) for theme-aware tints
--hot          #f04e7a        Pink-red; Hot status, validation errors
--warm         #f0a24e        Orange; Warm status
--cold         #4e8ff0        Blue; Cold status
--booked       #4ef0a2        Green; Booked status
--uncontacted  #4ef0db        Cyan; Uncontacted status
```

**Light theme** (`[data-theme="light"]` — July 13–16, 2026) redefines the same vars: `--bg: transparent` (the `<html>` gradient shows through), `--surface: #ffffff`, `--surface2: #f1f5f9`, `--border: #cbd5e1`, `--text: #1e293b`, `--text2: #475569`, `--text3: #64748b`, `--accent: #3b82f6` (blue replaces lime), `--accent-rgb: 59, 130, 246`, `--accent2: #0ea5e9`, `--hot: #dc2626`, `--warm: #ea580c`, `--cold: #64748b`, `--booked: #0d9488`, `--uncontacted: #3b82f6`, `--dead: #94a3b8`. `html[data-theme="light"]` paints a `linear-gradient(135deg, #dbeafe, #ffe4e6)` page background; panels/modals get frosted-glass + per-surface gradients via the `data-*` markers.

### ⚠️ The above is the LEGACY set — being replaced (July 30, 2026, branch `redesign/carved`)

The tokens above are on their way out. They stay only until their last consumer migrates, then die
individually. **New work consumes the `--c-` set below.** Spec: `docs/PRSFLO-DESIGN-SPEC.md`.

```
--c-bg          #1b1a17 dark / #f5f3ee light   Page ground (warm paper / dim room)
--c-fg          #d9d6cd dark / #2a2722 light   Ink. Warm ivory, NOT white
--c-wash        grouping fill, level 1
--c-wash2       grouping fill, level 2
--c-fg-2/-3     muted ink steps (migration equivalents for --text2 / --text3)
--c-chip-ink    #1c2626   Text ON status fills — same in both themes
--c-st-hot      #ff5a4d   Hot lead · urgent · cancelled session
--c-st-warm     #9d8cff   Warm lead · tentative session
--c-st-cold     #5fc9e8   Cold lead
--c-st-booked   #43dfae   Booked · confirmed · live room
--c-st-uncon    #7fb2e5   Uncontacted · tour
--c-st-dead     #cfd6d4   DNB · tech · open hours
--c-ivory       #d5d0c2   Dark-mode SMALL accents only (Law 5)
--c-hot-text    #fff4f2   Text on hot fills; every other chip uses --c-chip-ink
```

**Three things that will trip you up:**
1. **There is no accent colour.** Lime and teal are retired. Colour appears ONLY as lead temperature,
   session/calendar status, and analytics — always as a solid fill, never as tinted text.
2. **Polarity is inverted relative to the spec.** Dark values sit on `:root` (dark = the *absence* of
   `data-theme`); light overrides follow in `[data-theme="light"]`. Equal specificity — **the light block
   must stay after the root block.** The spec's CSS examples are printed the other way round.
3. **Status colours are identical in both themes.** Dark dims them by rule (§6) — a filter on pills, alpha
   fills on chips and pools — never by substituting a second palette.

**Depth is the grammar:** containers carve IN (`.c-panel`, `.c-inset2`, `.c-pool`), controls raise OUT and
press in on `:active` (`.c-raised`, `.c-raised-primary`, `.c-raised-chip`, `.c-control`), display anchors
rest on the surface (`.c-anchor`), latched controls stay pressed (`.c-pressed`). **No borders, dividers or
hairlines anywhere** — including 1px-tall spacer divs. No glows or outer halos; the only permitted outer
shadows are raised controls and the modal drop shadow.

---

## Roadmap

### Done

| Chunk | What shipped |
|---|---|
| **Tenants · the Mustard exception · two stale-page bugs ✅ (Sep 2–3, 2026, v1.21.0)** | **Tenants** — third word in the billing heading beside Billing/COD, own branch like Financials. Rent board (month picker, four figures, rooms grouped by venue, empties dashed) with the ladder **Mark sent → Mark paid → In QB**, each a stored stamp + ↩ undo; unpaid past the 5th goes hot. **Roster and rents are CODE** (`TENANT_ROOMS` in `lib/tenants.ts`, the nadines precedent); the only stored facts are the three human acts in `tenant_rent_months` (`20260902120000`). **The Mustard shared-runner sheet** — his ERS·B runner hours bill at HALF for any hour a billed ERS·A session ran; fully derived by intersecting studio-time rows (merged intervals; ERS·A filtered through the same `bookingShouldHaveWorkOrder` gate as the pipeline), **hours only** (Eli) → solo + shared ÷ 2 = billable; gap days render dashed, never zero. **Lockouts are the designated exception**: `woNeedsTimes()` exempts them from the missing-times rule, the runner hub selects `['confirmed','lockout']` (the ONLY operational surface that admits one), and the monthly modal's **Staff** choice (No staff / Assistant) is its own control instead of `eng_visible: !monthlyNA`. **Confirming needs times** — admin can't save a Confirmed WO while any dated studio day lacks From or To (`confirmStartProblem`); tentative saves freely; the runner banner only flags days that have HAPPENED and caps detail at 4 rows. **My Day** — `fetchDuties`/`fetchEntries` return **null** on failure (an empty array is an answer, a failed fetch is not) and the page keeps state, fixing the 2 PM "cleared my checkboxes" report; duty ticks moved to `opsToday()`. **Runner mics** — an initials-only draft no longer pins `dirty` (which had silenced realtime AND reload-on-return), and reload-on-return is no longer dirty-gated (the draft merge protects taps). **Runner home rebuilt** — no remembered studio, punch/guide/manual/report-a-bug moved to the landing, notes behind explicit studio tabs; petty cash gained "Counted — no change"; mic tile greyed with Coming soon (`MICS_COMING_SOON`, one-week hold). **Error sweep** — `/api/wo-package` returns named failures instead of anonymous 500s, `woPdf` is WinAnsi-safe and fails loudly on an unreadable attachment, `financials.priorBucket` clamps leap-day year shifts. Plus receipt thumbnails, an opaque WO notes popover, a `dvh` day sheet, and a seven-pill status bar. |
| **WO history · the approvals queue · the runner notes channel · actual vs billed ✅ (Sep 1, 2026, v1.20.0)** | **The approvals queue** (`lib/billing.ts approvalQueue` — membership IS `nextAction(r) === 'Approve'`, never re-implement the predicate): teal Rail badge on Billing (owners only, `c-rail-badge.c-ok`), owner dashboard banner (count + summed total), and an owners-only sweep strip above the hub lists with inline Approve (drift ⚠, informational PO chip). Realtime consolidated into `hooks/useWoInvoicesVersion.ts` (shared `work_orders`+`payment_rows` channel; the billing page's own channel retired onto it). **COD gained the owner's approval** — third light, Approve on paid+reviewed rows, `invoice_state='approved'` is the ONE stored state a COD WO carries (buckets stay computed); strip shows on both pipelines. **WO history** (`wo_activity`, append-only BY POLICY ABSENCE): one entry per save, TS diffs at the save choke points (`lib/woActivity.ts`), creation snapshot stored WHOLE as entry zero; the ladder is runner SUBMITS · admin REVIEWS (the per-row lock, now dbResult-checked) · owner APPROVES; viewer `WoHistoryModal` (⟲ in the letterhead, sentence feed, Original ⇄ Now compare); `after_invoice` ⚠ marks post-invoice edits. **The midnight pass**: `handleRunnerSubmit` + Submit footer + expense prefill → `opsToday()` (a 1 AM submit used to match no rows); manager note posts stamp the OPERATIONAL day; `opsDayOf(iso)` added to `lib/time.ts`. **Daily Ops**: flags carry `created_by_name` (checklist = TYPED initials, re-stamped on resubmit); the sweep splits **Opener / Closer** (petty cash is one duty shown under both), each shift's person from its own submissions. **Runner notes channel** (`runner_note_posts` — day-less, pure submit order, newest first): inline on the studio hub via `components/runner/RunnerNotesChannel.tsx` (composer above feed, RichNote, synchronous draft net, photos upload-on-pick into `checklist-photos/runner-notes/` with lightbox, own posts editable), Shift notes tile + page GONE (`shift-notes` is a redirect stub; docs backfilled in as history), admin view on Daily Ops (Office chip, `subscribe={false}` + `reloadKey`), sweep files posts by `opsDayOf` at read. **Actual vs billed** (`studio_time_rows.actual_from/to_time`): dashed Arrived/Left wells in the day sheet, quiet times-only card line, internal-only — no money math, never on the client PDF, history-logged. **Access/roster**: `invoices` bucket policies widened to `asst_manager` (delete stays owner-only); **Sam Jurequi replaced Quinn** (lib/tasks primary 'Sam' — display_name must be exactly `Sam`; open tasks reassigned, PIN deleted, login unlinked). Plus the day sheet's STUDIO header is now the list view's venue+room select. Migrations `20260901120000`–`160000`. |
| **Notes that survive · Billing Ops · the phone calendar · stock by location ✅ (Aug 31, 2026, v1.19.1)** | **BILLING RULING REVERSED: the PO blocks SENDING, not approving.** Owner approval asserts the WO is complete and the invoice correct — knowable the day the session ends, while a label's PO takes weeks. `awaitingPo` still derives from step 2 (chip warns during approval); only the hub's `blocked` moved to Download/Mark sent. Approve stays owners-only (`enforce_invoice_approver` trigger). **`myday_note_drafts`** (`20260831120000`) — the My Day composer autosaves; **one row per AUTHOR, not per date** (a draft is unfinished writing; the date belongs to the post Submit stamps), private to the author, `editing_post_id` persisted so a resumed edit updates instead of duplicating, cleared on submit — **cancel the debounce BEFORE deleting the row**. `dbResult` gained `{ silent }` (toast only; the caller must still surface the failure). **"Billing Ops"** replaces the person on the billing card (Aaron left — it is an unowned role until a rehire) and the **manager gets the `/my-day` card switch** (HR-SPEC §5.6; `completed_by` records who ticked). **Daily ops pages forward to Today** — unsubmitted duties read `not yet` in neutral and raise NO absence rows while the day is still running — and **Track is dormant** (`DORMANT_STUDIOS` in `lib/dailyOps.ts`: greyed, no absence rows, every code path intact; remove it from the array when staffed). **Runner submissions** ride on `app_feedback` with `source='runner'` + `studio` + `photo_url` (`20260831130000`), posted from the studio HUB (the picker bounces past `/runner`), draft-netted, read in DEV → Runner; the office board is scoped to `source='office'`. **Runner stock**: decimal keypad on qty (TEXT column — "IFAK"/"✓" must stay typeable), the **last prior day's count** shown with its date, and **grouping by LOCATION by default** with a Type toggle (`20260831140000` — 134 rows placed BY ID across eight locations; group order derives from each group's lowest `sort_order`). **Calendar on mobile**: teal occupied bar gone, **pinned** month picker with session dots, grid rows 79 → **26px** (option A, `docs/design-refs/calendar-grid-mobile-options.html`) so all 11 rooms fit one screen, chips carry the artist name only, and a tap opens a **read-only synopsis card** with Expand — chips carry NO minHeight at that row height or they overspill their lane. **Centring fix:** `marginLeft: -12` on the calendar page is a DESKTOP rail pull and now runs only there. **RichNote** draws its own bullet (WebKit doesn't paint native markers in contentEditable) and its B / • buttons show real state. See CHANGELOG v1.19.1 WATCH-OUT #1 for the sandbox-commit incident that deleted the v1.19.0 docs twice. |
| **2026 import · lockouts · the 8:50 operational day · notes that survive ✅ (Aug 26–31, 2026, v1.19.0)** | **2026 WordPress calendar imported** (`scripts/importCalendar2026.mjs` — snapshots then DELETES all `bookings` rows first, approved as test data). **`bookings.imported_at`** (`20260826150000`) is **provenance, not state**: past+imported = read-only history (no WO, no invoice number, no daily ops/runner hub); today-or-later+imported = a legacy WO-less block the promotion path adopts, and promotion does NOT clear the stamp. Imported rows are excluded from My Day's missing-WO alarm (that alarm means a WO *failed to create*). **`'lockout'` BookingStatus** — rent-only monthlies: occupied, payable, nothing daily; **excluded BY STATUS** (every ops surface selects `confirmed`, so no exclusion flag belongs anywhere else) but NOT in `NON_SESSION_STATUSES`, because rent gets invoiced. **Monthly split**: a flat monthly allocated across dated studio rows by largest-remainder, exact to the cent; rows then behave as ordinary day-rate rows; OT typed per deal (never the autofill). **`DAY_HOUR_RATIO = 10`** (`lib/woTotals.ts`) — day = 10× hourly, day-row OT = day ÷ 10, twin-field sync; **no exception list, ever**. **3% card surcharge** (`payment_rows.fee_amount`, `20260826160000`): `amount` = what hit the card, `fee_amount` = the slice inside it via `cardFeeOfCharged`; added to the grand total so a card payment moves the balance by its base; WO prints "If paying by card (incl. 3%)". **`opsToday()`** (= `shiftLogDate()`, 8:50 AM LA) is the day key for **every runner surface** — sessions/WOs survive midnight, checklists file under the night, Wednesday office gate holds; **punches stay on the calendar day** (payroll). `getLocalToday()` is calendar semantics only. **Shift notes v4 → `shift_note_docs`** (`20260826140000`): one big autosaving field per runner per shift, 8:50 seal kept, replaces the never-adopted `shift_log_entries`. **`RichNote.tsx`** — the ONLY rich-text surface (bold/bullets/tab, contentEditable + `execCommand`, whitelist `sanitizeNote()` at edit-sync and display, paste forced plain, legacy text unchanged, no migration). **`runner_section_notes`** (`20260828130000`) — one shared note per (studio, ops day, section), surfaced in Daily Ops. **`myday_note_drafts`** (`20260831120000`) — manager composer autosaves; **one row per author**, not per date; private to the author; `editing_post_id` persisted; cleared on submit (cancel the debounce first or it resurrects the post). ARS test pass: Soundstar → ARS, Fridge section, mic test-data wipe, Odds/Genelec quantities (`mics.has_qty`), `RunnerGuard` defers expiry while typing. Plus registration `profile_confirmed_at` fix, `leasing` lead status (orchid, aliased to `--c-st-tech`), the iPhone dashboard/calendar pass, and `dbResult` `{ silent }` (toast only — the caller must still show the failure). |
| **Runner feedback pass + the mic Sheet ✅ (Aug 25, 2026, v1.18.0 — mic Sheet merged to `main` Aug 26)** | Everything from the runner's Aug 24 test: stock data corrections via `20260825120000_stock_runner_feedback.sql` (six items → Cleaning; PRS-X list = Chobani Sweet Cream Creamer [renamed from Vanilla, history kept] + Mini Half n Half + 2% Milk; Pike Place K-Cups; bagels Plain/Everything/Cinnamon; junk "C" row deleted; `mics` AKG 3000B typo). **Stock add-item bug fixed** (name input flipped to a read-only span on the first keystroke; unsaved rows now stay editable + get an × to discard). Checklist office run → **(Wednesdays)**. **Initials auto-fill from the login profile** on checklist + mics (canonical `profileInitials()` in `lib/format.ts`); shared runner account still types. **Hint tips portal to `<body>`** (inherited `opacity` from `.c-label` dimmed them — no background can fix inherited opacity). **Mic inventory rebuilt as THE SHEET** (Option A, `docs/design-refs/mic-inventory-options.html`; catalog snapshot `mic-catalog-2026-08-25.json`, 272 items): per-studio tabs w/ progress counts, birdseye tap=HERE grid, Room/Missing/Clear popover (fixed-position), pinned search, missing-streak alert, display-only last-night refs (4-day + 21-day missing windows — stay under PostgREST's 1,000-row cap). Jump-to-top root cause: components defined inside the page component (remount every render) — never do that. Data model + save/submit upserts unchanged. |
| **Stock v3 + runner drafts + WO food budget + Complete↔Reopen ✅ (Aug 24, 2026, v1.17.0 — wrap-up written Aug 25)** | **Stock page rebuilt** (Option C + landing): PRS Stock / Office two-button landing (office Wednesdays-only, DUE TODAY pulse), collapsible category groups, one-line rows, tap-to-expand notes + per-date history in new **`stock_checks`** (qty is TEXT — "0.5", "IFAK", "✓"); `stock_items` stays the current-state mirror read by `lib/dailyOps`/`DailyOpsModal`; real Paramount lists seeded (placeholders WIPED), bagels split per flavor, categories regrouped. **`lib/draft.ts`**: stock/petty-cash/mics mirror typed input to localStorage, restore on return, clear only on successful save; checklist attention-notes flush on unmount — standing pattern for every runner batch surface. **WO food-budget expense report** (`wo_expenses`): runner-fillable rows + receipt photos, writes IMMEDIATE (never the WO's batched save), receipts → `checklist-photos/wo-receipts/` + printed into the invoice PDF package. **Complete flips to Reopen** (confirm-guarded, restores OPEN, pulls back out of needs_invoice). Red **Balance due is COD-only**. WO client search "+ New client"; COD artist field; shift notes v3; Clients tab carved; calendar + New Booking button removed. Migrations `20260824120000`–`180000`. |
| **iPhone icon fixed — Vercel Firewall rule ✅ (Aug 22, 2026, v1.16.2)** | iPhone Add-to-Home-Screen showed a letter tile because **Vercel Bot Protection challenges non-browser sources** — which iOS's background icon fetcher is; iOS 26 falls back to a letter tile with no error anywhere. Fix: dashboard Firewall rule **"Allow PWA icons"** (Bypass for `/apple-touch-icon`, `/icon-`, `/runner-`, `/manifest.json`, `/favicon` path prefixes). **Lives in the Vercel dashboard, not the repo** — see ONBOARDING §5 landmine + PROJECT_LOG Aug 22 for the full elimination path. Icon assets now ship square/opaque + maskable with `-v2` URL aliases (debug fallout, kept). |
| **The Ribbon — brand mark recoloured ✅ (Aug 22, 2026, v1.16.1)** | The carved monochrome wave mark is retired for **one solid twisted-ribbon wave in sea green `#43dfae`, fixed in both themes** — spec **§20**, the second sanctioned Law 3 exception (the Flo glow being the first). Picked from `docs/design-refs/brand-mark-options.html` (colour A · Lagoon → construction G1 · Ribbon; B/C/D/E and G2/G3 rejected). **The path lives identically in `components/PRSFloIcon.tsx` AND `scripts/generate-icons.js` — change both together** and re-run `node scripts/generate-icons.js` (10 assets; runner set = warm amber `#ffa94d` on the same charcoal ground). `Wordmark.tsx` casing restored to `PRS`+`Flo` (was `FLO`); Archivo Black kept after a Syne/Inter/DM Mono audition (F1–F4). Installed PWAs may cache the old icon until re-added. |
| **Financials: nine years of revenue ✅ (Aug 20, 2026, v1.16.0)** | Owner-only revenue chart as a **third title beside Billing and COD** (`view` state, deliberately not a `Pipeline` value), plus the 2017–2026 archive out of the "PRS Daily Numbers" workbook — **55,601 rows, $51,744,630**. **History is its own frozen table, NOT synthesised work orders** (`financial_history`, migration `20260820140000`; owner-only SELECT, no write policy, unique `(source_key, category)` so re-imports upsert) joined to live money at read time in `lib/financials.ts`. **Never `select` raw rows from it** — PostgREST caps at 1,000 silently, which drew nine years as one 2017 bump; `financial_monthly()` / `financial_rooms()` / `financial_latest_date()` (migration `20260820150000`, **SECURITY INVOKER** so the owner policy still governs) roll up in Postgres. **Storage is DAILY on purpose:** the partial current month compares against the same day range a year earlier (`amount_to_day`), not a whole month. Chart is **one line with a swappable metric** (Eli's ruling — a stack made engineering/rentals flat smears under 85% room revenue), a **comparison picker** (previous year, or any year pinned as a baseline), a **Years overlay** (Jan–Dec axis, line per year, single-hue ramp), brush zoom, room dropdown incl. the seven retired rooms. Import is two steps: `scripts/extract-financial-history.py` (workbook → CSV per year, reconciled day-by-day against the sheet's own roll-up columns — found 124 self-inconsistent days) then `scripts/import-financial-history.mjs`. Also removed `srv2129@gmail.com` from all nine `isEli` checks. **Open modelling questions in `docs/BILLING-MODEL-OPEN-QUESTIONS.md`** — assistant is not itemized, rentals mix gross and net. |
| **Launch night: PIN outage + WO staffing repair ✅ (Aug 20, 2026, v1.15.1)** | **PIN login fixed** — three stacked faults: `$2b$` hashes pgcrypto silently no-matches (live prefix rename; `set-pins.mjs` now forces `$2a$`), a `/^\d{4}$/` guard in `app/api/auth/pin/route.ts` rejecting 6-digit PINs pre-lookup, and a catch-all client message hiding both (the pad now prints the server's code). **PIN length lives in 3 files — route regex, `PIN_LENGTH`, `set-pins.mjs`** (CLAUDE.md rule). **WO staffing repaired after a no-code audit**: first staff line is the room row's own slot (`eng_visible` defaults true; card/sheet filters dropped the name/rate requirement that hid "engineer, TBD"), new `staffTimes(row)` resolves staff times live via the date's room row (standalone rows previously showed blank forever), standalone rows reserved for extra staff, room rate + `/HR·/DAY` and each staffer's rate moved under the times they price, × on every staff block (standalone deletes, room slot hides). Deliberate non-change: nothing auto-reveals a hidden slot. **Also:** equipment pills cycle back to blank (both surfaces; Not OK notes survive clearing), time-picker chevron out of tab order, New Lead dropdowns opaque, registration banner rebuilt as option D (`docs/design-refs/reg-banner-options.html` — per-person rows, one-tap confirm, `regPulse`). |
| **LAUNCH + day-one wave ✅ (Aug 19–20, 2026, v1.15.0, merged to `main`)** | **The redesign is LIVE in production** (merge conflict: both branches had added the runner sign-out — carved styling kept). Day-one fixes from real use: **Billing** — `upcoming` bucket deleted (`lib/billing.ts`: everything pre-send is `progress`, COD gains the tab, `InvoiceRow.notStarted` chip + split sort), COD tabs are **latches** (multi-bin merged list, per-row `.c-bbin` badges, persisted, last latch stays; Billing keeps single tabs). **WO desktop** — title band deleted (letterhead is the header), overlay clears the 176px rail + full height, doubled flat action buttons, `c-seg-tiny` one-row segs, day-card left region 340px, admin-only day/staff delete ×s. **Runner** — `components/runner/RunnerGuard.tsx` gates `/runner/*` (`/runner/sop` public) + expires runner sessions (4h / 8:50); PINs back at **6 digits** (`scripts/set-pins.mjs` deals unique PINs to everyone + rotates all passwords; `PIN_LENGTH=6`); identity visible (landing greeting, hub header, punch "Submit as NAME"). **Shift log** — tap-to-edit + `edited_at` marker while live, sealed 8:50 AM (`shiftLogDate()` in `lib/time.ts` + `shift_log_edit_live` RLS policy, migration `20260820130000` — keep the two in step; also fixes the midnight split). **SOPs** — `public/{billing,manager}-sop.html` served at `/sop/billing` + `/sop/manager`, Training page is the learning corner (SOP + hints moved from Settings), billing SOP WO chapter redrawn to the launched layout, `public/sop.html` re-skinned (content + VERSIONS untouched). **Misc** — CRM desktop at `zoom: 0.9`, room cards 84px, `app/register/layout.tsx` link previews "Paramount Client Registration", truthful login errors, `NEXT_PUBLIC_BASE_URL` set (env-var outage postmortem in PROJECT_LOG). |
| **WO reorganization — admin desktop two columns ⏳ (Aug 18, 2026, v1.14.0, task #11, branch `redesign/carved`)** | The work order splits into **words left (`0.72fr`) / numbers right (`1.28fr`)** on admin desktop, built to `docs/design-refs/wo-compact-options.html` (its header comment is the ruling list). **LEFT:** open-space letterhead top-left (branding + address, big WO number + invoice # + status) replacing the centred masthead → ★ locked contact card (LABEL is hero for label clients, artist well sized to content, A&R **beside** Admin, emails ellipsize, Email/Call/Text → ✉ ☎ 💬) → Invoice/PO/Food row → Booking notes (INTERNAL + wash2) → Session notes (plain) → Needs attention as a bottom-pinned strip that **grows only when it has content**. **RIGHT:** studio-time bin and rentals bin scrolling **independently**, each with a live `ScrollHints` (fade + "↓ N more", shown only while content remains below) → pinned itemized total (Studio + a line per engineer/assistant + OT note + arch total) → payments/totals, pinned. Per-day song notes moved into each desktop day card's middle column from the existing `session_info`. Sheet cap 920 → **1320** for this layout only. **Zero logic:** no save, query, realtime, atomic RPC, projection or `lib/woPdf.ts` change — the PDF is drawn server-side from `studio_time_rows` and never reads this DOM. **Mobile/runner byte-identical** via `display: contents` on both column wrappers + an explicit `ORD` flex-order map. `ClientPanel` gained `layout?: 'stack' \| 'wide'` (default `stack`, all other callers untouched). No migrations. |
| **Launch prep: SOPs + hints + Settings + Engineers ⏳ (Aug 17, 2026, v1.13.0, branch `redesign/carved`)** | **Helpful hints app-wide**: `components/ui/Hint.tsx` (`Hint`/`useHints`/`setHintsEnabled`, localStorage `prsflo-hints`, default ON) + `.c-hint` styles; toggles in the rail Settings disclosure and the runner hub 💡; seeded ×9. **Rail**: foot → ⚙ Settings (SOP · DEV · hints · theme · Sign Out); Admin de-nav'd; `/engineers` under Operations (roster rebuilt: soft skin, `engineers-page` realtime channel, dbResult writes). **Runner app SOP** `/runner/sop` ← `public/runner-sop.html` (chaptered phone guide; "Runners manual" slot = Eli's separate job manual, still Coming soon). **SOP mocks** `docs/design-refs/{billing-sop,manager-sop}.html` — chaptered flow-first manuals with real Flo box + real WO walkthrough + oversight chapters. **Daily Ops**: queue paged at 10; sweep date-hero + ‹ ›/swipe day browsing (replaces the ops log). **Day-not-night** wording ruling applied app + SOPs. Deleted `/preview` + `DeviceToggle`; Runner Hub rail link → `/runner`. Ops SQL (hand-run): `supabase/launch_reset_20260817.sql`, `scripts/create-runners.mjs`. |
| **Day sheet final + live-merge ⏳ (Aug 16, 2026, v1.12.0, branch `redesign/carved`)** | Day sheet rebuilt to the approved pair-block mock (`docs/design-refs/runner-wo-day-sheet-final.html`): studio + staff in matching blocks, big time wells (type or ▾ half-hour dropdown opening AT the shown time), hours chips, >14h AM/PM warn-only tripwire, swipe/chevrons between days (approved days view-only), flat itemized billing with staff rate wells (office-editable). **OT = time beyond the agreement**, derived from the clock on runner edits (hourly: actual − booked hrs; lockout: past 12h) — never typed; pre-approval flag mocked then dropped by ruling. **The open WO is now LIVE both ways**: `wo-live-<id>` channel on all six tables → debounced per-field merge where unsaved local edits win (baseline = `originalStRowsRef`; channel callbacks read ref mirrors — see CHANGELOG watch-outs). Hub pill answers the runner ("Not submitted / Submitted / Approved"); `hooks/useReloadOnReturn.ts` re-fetches five runner pages on foreground return. Runner always defaults to cards. |
| **Runner work order = WorkOrderPopup runner mode ⏳ (Aug 16, 2026, v1.11.0, spec §15, branch `redesign/carved`)** | `app/runner/[studio]/wo/[id]/page.tsx` shrinks 1,595 → 136 lines — a wrapper that resolves the WO + booking and renders the shared `WorkOrderPopup` with `mode="runner"` (+ `runnerStudio`/`runnerStudioLabel` for the flag rows). **Field-level locks, not read-only:** locked Session Info top (A&R `tel:` pill, Balance-due chip), rates as text everywhere, `admin_locked` days inert, no batch/seed/add/delete/Complete; times, staff, OT, §18 equipment pills, song titles, payments, rentals, notes, NA photos and COD signature stay live. **Submit footer** = the same `save_work_order_atomic` + projection, then today's rows → `'submitted'`; re-opens stay editable ("Update submission") until office approval. Flag sync runs in every runner save. **List ⇄ cards toggle for BOTH modes**: cards = one day per card → bottom day sheet (admin sheet has rate inputs; runner sees the read-only billing block); default cards on phones for ≤3-day sessions. `StRow.status` carried state-only (never in the save payload). Runner copy tracks the clock via new `lib/time.ts` `dayPart*` helpers (24/7 operation). Mock `docs/design-refs/runner-wo-views.html`. No migrations — but verify `studio_time_rows.status` in the live DB before trusting Submit. |
| **RLS actually enforced ⏳ (Aug 14, 2026, v1.10.0, branch `redesign/carved`)** | The July 2 hardening had **never taken effect**: it created the tiered policies but only dropped the five legacy `Public access` ones it named, leaving ~40 older open policies — `authenticated access` (ALL) on ~20 tables plus anon-reachable `Public`/`open access`, including `user_profiles`. RLS policies are PERMISSIVE and OR together, so one open policy voids the model. `20260814130000_drop_legacy_open_policies.sql` drops them **dynamically** (any policy whose `qual`/`with_check` is literally `true`, minus a five-table allowlist: `app_feedback`, `dashboard_task_comments`, `studio_tasks`, `test_results`, `venue_open_items`) — a name list is what missed them the first time. Procedure: drop → re-run `20260702161117` in full (idempotent) → verify against `pg_policies`. Anon reads confirmed dead after. |
| **Runner hub grows up ⏳ (Aug 14, 2026, v1.10.0, spec §15b)** | Option A · Sections from `docs/design-refs/runner-hub-additions.html`. `studio_tasks` (per-STUDIO, never per-person — runners rotate) as a section above sessions; punch + manual as a quiet bottom register; **session cards now render the shared `SessionCardBody`** (new opt-in `large` prop — calendar/dashboard output byte-identical when omitted), so runner cards carry the calendar's status colours, COD strip, invoice and staff tags. One-landing merge: `/runner` remembers the studio and skips the picker, header switcher pills, `?choose=1` un-remembers. Soft-skin port of checklists / mics / petty cash / stock — surface only, every query, `dirtyRef` guard and flag-raise untouched. `components/runner/AdminReturn.tsx` gives the office a way back out of the runner subtree (renders nothing for runners and the public). |
| **Missed punches ⏳ (Aug 14, 2026, v1.10.0, HR-SPEC §5)** | `punch_correction_requests` + `set_punch_report_class()` — same-day vs late is derived by the DB from `submitted_at` vs `shift_date`, never client-chosen; no DELETE policy (it is the legal written confirmation California requires). `/runner/punch` = form + the person's own 90-day record, colour-banded; `/punches` = manager queue (approve / adjust time / reject with reason), "Enter in ADP" list with the auto-composed PRG-P01 comment + copy, and per-person counts. **Counts, not points** — a "% of shifts punched correctly" needs shift counts that arrive with scheduling; `lib/punches.ts` is the single source of labels and colour bands so the upgrade lands everywhere at once. The shared runner login is refused rather than filing an unattributable record. |
| **Daily Ops + shift logs ⏳ (Aug 14, 2026, v1.10.0, spec §19)** | `/daily-ops` fills the empty rail placeholder: queue LEFT (flags → **absences** → missing mics → notes; tap to clear; "Yesterday is done" when empty) with the studio-task manager beneath it, 2×2 studio sweep RIGHT with shift-log popup, night pager. `lib/dailyOps.ts` computes it — **an unsubmitted duty is now a hot item; it used to render as nothing at all.** `shift_log_entries` replaces the Slack shift-notes post: append-only, per studio per night, multiple authors (mid-shift handoffs), no UPDATE/DELETE policies. `daily_ops_reviews` persists cleared items by `(date, item_key)` — **those keys are load-bearing, renaming one orphans its state**; flags clear by acknowledging the flag instead. Rail regrouped: Dashboard · Calendar · My Day, then Business, then **Operations**; "Studio" retired. |
| **The work order PDF becomes the work order ⏳ (Aug 13, 2026, v1.9.1, branch `redesign/carved`)** | `lib/woPdf.ts` rebuilt as a section-by-section replica of `WorkOrderPopup` — same twelve studio-time columns, engineer sub-rows, rentals, notes, payments, six totals, COD terms. **Session Info wraps and grows the row with no line cap** (the one deliberate divergence from the screen, and it shows MORE: paper has no popover). **Internal fields are deleted from the record before drawing** via `INTERNAL_ONLY`, so Booking Notes / Needs Attention cannot leak by a future edit. **Blank fillable form** at `/api/wo-package?blank=1` (188 AcroForm fields) behind a page-level `⋯` on `/billing` — creates nothing, stores nothing. Invoice pages now come FIRST in the merged package. Studio column prints `PRS A` / `TRS North` via new `lib/studios.STUDIO_SHORT` + `roomCode()` (**Track is TRS**; three private copies of that map deleted). **Verified by rendering PDFs and inspecting them — `tsc` was clean on a generator that never drew engineer sub-rows.** Migrations `20260813120000` (wipe stale snapshots — POINTERS only; Supabase's `protect_delete()` blocks SQL deletes on `storage.objects`) and `20260813130000` (dedupe staff rows). |
| **Admin WO workflow — five bugs + two warnings ⏳ (Aug 13, 2026, v1.9.1)** | Found by tracing the workflow end to end; four share a shape — *a control that looks available and does nothing*. **A drifted invoice could never be approved** (approve wrote step 3, `deriveStep` demoted it to 2 off the stale `invoice_total`) — `approveInvoice` re-snapshots the total now. **Complete WO never saved or closed** despite a comment claiming it did; `handleClose` returns a boolean and Complete refuses to stamp a failed save. **Four fields never marked dirty** (session notes, print name, needs attention, signature). **Equipment condition only worked on day one** — rows are seeded at creation for the dates the booking had then, so later days had no row and the handler was gated on `row &&`; first tap creates it, and the write is `dbResult`-checked. Buttons swallow double-clicks. New live warnings: an **engineer** line with hours and no rate (assistants exempt — never rated), and the **same person on two lines for one day** (found in live data, WO-1018, $880 double-billed). **`bookings.engineer_rate` is DEAD — five reads removed; `woTotals.fallbackEngRate` gone.** |
| **Table chrome is text, not bars ⏳ (Aug 13, 2026, v1.9.1, spec §16/§16c)** | Studio Time groups a DAY and its staff into one wash block (zebra retired). Then: column headers and footers lose their fill entirely, subtotals sit in a wash2 result chip, and **only entries are filled and rounded** — one radius per surface, so no seam. Equipment regrouped along its own axis (one bubble per piece of GEAR across every night, not per day), which is why it had looked worst. Billing's search bar dropped its 99px capsule for `.c-input` geometry. §16b ("round every strip") superseded but kept — the reasoning for why the first fix chased the symptom is worth more than the fix. |
| **Runner hub — Day card ruling + landing ported ⏳ (Aug 13, 2026, v1.9.1, spec §15)** | Eli picked option A from `docs/design-refs/runner-hub-options.html`: the work order's twelve-column sideways-scrolling table becomes ONE CARD PER DAY, rates/OT/day-total read-only ("set by the office"), and the runner's terminal act is **Submit**, never Complete. `/runner/[studio]` ported to the soft skin — every query, both realtime channels and the booking→WO resolution untouched. **`/runner/[studio]/wo/[id]` (1,591 lines) is NOT built yet.** |
| **Work order surface pass ⏳ (Aug 13, 2026, v1.9.1, spec §16c/§17/§18)** | Section headers are TEXT app-wide (`.c-lozenge` lost its fill — and three rules that leaned on it: the count badge inverted against it, the action link took its contrast colour, and `SectionHeader`'s `.c-anchor` shadow became a smudge). **Equipment condition folded into the studio day** as a third line, one pill per item, tap cycling blank → OK → Not OK → OK and never back to blank; the separate date-column table is deleted along with `fmtDate`/`sessionDates`. WO meta: PO widened and beside Inv #, the PO field answers its own question via a `Not req'd` chip in the well, food is just an amount. Header stays put while the body scrolls (`.c-sheet` has `overflow: hidden`, so sticky was bound to the sheet, not the page — it is a flex column with an overflowing body now). One action bar: Cancel · Complete WO · Save. `/billing`: the pipeline IS the title with live counts, COD hot on a balance. **WATCH-OUT — the scoped `.c-sheet button:not(…)` / `input:not(.c-input)` defaults out-specify a plain class**: any new in-sheet control must be added to the `:not()` list or it renders oversized with no status colour, and any wrapper-styled input needs `.c-tin`. |
| **Purge ⏳ (Aug 13, 2026, v1.9.1)** | `/wo/[id]/print` **actually deleted** — CLAUDE.md had claimed so since June while the route still shipped and still described the work order's layout a second time. Dead `fallbackEngRate` parameter removed. Two wrong CLAUDE.md entries corrected. `.claude/worktrees/` (abandoned June copy, 23MB) documented as an ONBOARDING landmine and **deliberately left in place** — gitignored, skipped by tsc because `**/*` does not match dot-directories, never bundled; the hazard is a grep hitting a stale duplicate. |
| **New-inquiry email alert ✅ HOTFIX to `main` (Aug 6, 2026, v1.6.2, `95dfe98`)** | The inquiry form used to live on Squarespace and emailed the team, so a lead arrived as a phone notification; moving it into PRSFlo removed that. Every web inquiry now also emails `info@paramountrecording.com` with `reply_to` set to the customer. `lib/sendMail.ts` — plain `fetch` to Resend, no npm dependency; the verified sender hoisted out of `/api/send-campaign` into `MAIL_FROM_DEFAULT`. **Send is after the insert and its result is ignored on purpose** (a lead lost to a mail outage is worse than a lead with no email), and `await`ed rather than fire-and-forget because a serverless function can be frozen the moment it returns. **Found in the process: `RESEND_API_KEY` had never been added to Vercel and the sending domain had never been submitted for verification — so email campaigns had never worked either.** Both now done. **Deferred:** full web push (service worker + VAPID + subscriptions) for company-wide notifications — inquiry alerts, runner session changes, staff blasts, policy memos; scoped but not built. |
| **Default theme flipped to DARK ⏳ (Aug 6, 2026, v1.6.0, branch `redesign/carved`)** | `app/layout.tsx`'s pre-paint script sets `data-theme="light"` only when the user has explicitly saved it (was: light unless `dark` was saved); `Nav.tsx`'s mount effect matches — **the two must always agree or the page paints one theme and flips on hydration.** PWA `theme_color` moved off the legacy `#0d0f14` in both manifests. Welcome splash fixed: it was being overpainted by a legacy `[data-splash]` / `[data-auth-hold]` rule using the retired blue→rose gradient; the AuthGuard hold moved to `var(--c-bg)` and the rule was deleted. |
| **Carved redesign — calendar FROZEN + Work Order migrated ⏳ (Aug 6, 2026, v1.6.0, branch `redesign/carved`, NOT merged)** | **Calendar frozen by ruling** — grid, cards, COD strips, per-day staffing, long-bar payload, hover card, sticky month rail; no further calendar work. **One session card for four surfaces** (`components/calendar/SessionCard.tsx`) — calendar grid, day view, studio view and the dashboard room grid each had a drifted copy. **`sessionFillClass()` is the only status→colour decision in the app** (the dashboard was painting tech, tour, open hours and *cancelled* green). **`--c-st-tech`** orchid added → seven status colours where spec §3 lists six. **Continuous horizontal zoom** (trackpad pinch / `[`·`]` / `− 14d +`, persisted) replaces Week/2 Wks/Month; row height reduced to two modes. **WO popup migrated end to end** — shell, header, footer, body, meta, client panel, four tables; **IdWell** (§8) short-ID fields share rows with the label inside the well; **segmented controls** are one housing; **TABLE EXEMPTION** (§8) — no wells inside data tables, bare cells + zebra + `TCELL_X = 6` shared inset. **WO print un-broken** (blank since `2e67ec0` un-portaled the popup). Entered-value weight 500 app-wide. `[style*=` metric 32 → 30. **Three features were silently defeated by `.c-sheet` scoped-default specificity (0,7,1) before this was understood — see CHANGELOG watch-outs.** **Remaining:** admin, WO Hub, runner, `/tasks`, welcome splash, and `public/sop.html` VERSIONS owed at merge. |
| **Runner sign out ✅ HOTFIX to `main` (Aug 6, 2026, v1.6.1, `4b74ae2`)** | `/runner/*` sits outside the nav by design, and `Nav.tsx` was the **only** caller of `supabase.auth.signOut()` in the app — so a phone signed into the Runner PWA was signed in permanently (installed app starts at `/runner`; `/login` bounces authenticated users to `/`). Sign Out added under the studio cards on the Runner Hub landing. Cherry-picked from the redesign branch; **the only commit from that branch in production.** |
| **"Carved" design system — tokens, primitives, first four surfaces ⏳ (July 30, 2026, v1.6.0, branch `redesign/carved`, NOT merged)** | Replaces the light-mode patch layer (63–66 override rules, ~20 matching inline-style substrings) with a real token + primitive system, both themes first-class. **Tokens:** the `--c-` set (permanently prefixed — see Design system tokens above). **Primitives:** `components/carved/index.tsx` — Button, SoftButton, Input, Panel, StatusPill, StatusDot, Count, RoomCard, EventChip, Row, Table, Modal, NewLeadPulse + `toCarvedStatus`/`statusFillClass`; `SectionHeader` and `StatusBadge` **extended** with a `carved` prop, never duplicated. **Style guide:** `/dev-style`. **Wordmark:** extracted to `components/layout/Wordmark.tsx` (Archivo Black, monochrome) — the locked `CLAUDE.md` rule changed from "copy these spans" to "render the component"; `PRSFloIcon` went monochrome and dropped `'use client'`. **Surfaces migrated:** dashboard + all seven modals, `LocationStrip` + daily-ops drawer, `DailyOpsModal`, nav chrome, login, reset-password. **31 legacy override rules deleted.** No migrations — presentation only. **Remaining:** CRM, calendar, admin, WO Hub/popup, runner, `/tasks`, welcome splash. **Track progress as `[data-theme="light"]` selectors without `.c-` — 36 left, from ~66** (NOT the `[style*=` count; 38 of those live in the WO print block). |
| **PIN login taken down after a brute-force attempt ✅ (July 29–30, 2026, v1.5.0)** | `POST /api/auth/pin` took sustained guessing from **~50 distinct IPs**, spread so no single IP tripped the per-IP lockout. Root cause of the exposure: **`verify_staff_pin(p_pin)` takes only the PIN** — no username — and matches every staff row at once, so 4 digits against ~10 accounts is a ~1-in-1,000 hit rate per guess. Root cause of the *cheapness*: the lockout **reset `fail_count` to 0 on lockout**, handing out 5 fresh attempts every 30s forever. **Fixed** — counter only climbs, escalating `30s → 2m → 10m → 60m` (capped at 60 deliberately: studios share a public IP), 6-hour quiet-period decay, new append-only **`pin_login_failures`** (migration `20260729140000`; no insert policy — service-role writes only). **Then removed entirely** while the app is pre-launch: all `staff_pins` deleted, sessions revoked, everyone on email + password, **`PIN_LOGIN_ENABLED = false`** in `app/(auth)/login/page.tsx` (single flip to restore). New **`scripts/set-one-password.mjs`** (admin-API password set, no email — Supabase's built-in sender caps at ~2/hour; rejects macOS curly quotes). Vercel **Bot Protection → Challenge**. Also: registration ID viewer no longer auto-downloads (`isPdf` gates the iframe); client-facing forms forced dark. **Planned replacement:** device-bound PIN — sign in once with email + password, then the session is *encrypted with the PIN* so nothing usable sits on the device. |
| **Runner WO lookup fixed for projection cards ✅ (July 30, 2026, v1.5.1)** | The runner resolved a booking's WO via `work_orders.booking_id`, which since the WO rebuild names only **one** of the several projection cards a save writes — so every card except the original dead-ended on "Work order not yet created". Now reads **`bookings.work_order_id`** first (both `app/runner/[studio]/page.tsx` `woMap` and the WO page resolver), with the reverse lookup kept as a fallback for pre-rebuild rows. **`work_orders.booking_id` is now a fallback, not the primary link** — still load-bearing for create-idempotency until Step 9. Mobile WO sheet offsets `top: 52` below the Nav (desktop always did; `100dvh` → `100%` so the footer isn't pushed off-screen), and the leftover per-studio header accent was removed — it mapped `ameraycan` → `var(--hot)`, the danger red. |
| **§14 frame + SOFT skin (Aug 7–10, 2026, branch `redesign/carved`, v1.7.0 UNRELEASED)** | Side nav rail replaces the top nav app-wide (`components/layout/Rail.tsx`, 176px, hamburger drawer on mobile; `Nav.tsx` dead-not-deleted); placeholder pages for Daily Ops/Punches/Hiring/Training + thin `/flags` and `/mic-inventory` hosts; command-row dashboard (Pipeline indicator + pulsing new-inquiry bar, 4 studio cards, Flo box w/ Aurora ring, static My Day stub beside LIVE personal tasks, static staff grid, 12-room sessions pane; LocationStrip retired); §2b density law (compact primitives, one-line lead rows); **§7c SOFT skin replaces carved — one reversible override block at the END of `globals.css`**; WO-table gutters + `.c-tin-show` field chips. Key new files: `components/layout/Rail.tsx`, `components/layout/PlaceholderPage.tsx`, mocks `docs/design-refs/dashboard-{console-v2-options,indicators-options,flat-skins}.html`. |
| **My Day — operational duties layer (Aug 10, 2026)** | Brief `docs/MYDAY-BUILD.md`; design `docs/design-refs/my-day-final.html`. Four tables (`myday_duties`/`myday_entries`/`myday_queue_steps`/`myday_notes`, six migrations `20260810120000`–`20260810170000`) + `lib/myday.ts`. Live on three surfaces: the **dashboard card** and **Flo briefing** (replacing `MYDAY_STATIC`/`FLO_STATIC`), the **14-day staff grid** (replacing `DGRID_STATIC`), and **`/my-day`** — the Workbench page with duties by cadence, role-specific queues, the manager's read-only billing peek, and shift notes. Two role cards, manager + billing; **billing is a ROLE**, so a successor inherits it untouched. Dashboard view-as gained a third option where **`eli` is oversight with no duty card of his own**. Rulings that shape it: plain checkboxes, no roll-up or override (lazy-tick risk knowingly accepted); day-scoped duties show only on their day with a Flo lookahead the day before; overdue shows IN PLACE, never relocated to a band; monthly duties stick and escalate rather than tally; **indicator vs checkbox** — if the app can determine it, it's a derived light, only what it can't know is a pill. |
| **Unified Work Order (Booking = WO) — ✅ FEATURE-COMPLETE (July 28, 2026; steps 8–9 cleanup deferred)** | Collapsing the two-form model into one Work Order that is the source of truth; `bookings` demoted to a calendar projection. Master plan `docs/WO-SPEC.md`. **Done:** `wo_number` (WO-1001…) + `bookings.work_order_id` + session-level fields + `booking_notes` migrations; `components/shared/ClientPanel.tsx`; `lib/seedStudioTimeRows.ts` (append-only Seed helper); rebuilt WO top (WO number, colored status bar, session type, billing, Booking Notes, ClientPanel) with per-day schedule removed from the top; collapsible Seed panel; Step 5a projection (studio+dates+times+`work_order_id` synced to the card, `roomLabelForVenue` bare-letter→full-label). Commits `3f64d95`→`c540614`, all on `main`. **Remaining:** 5b multi-room card splitting · 6 calendar opens WO directly (kills the extra click) · 7 richer card · 8 delete `BookingForm.tsx` · 9 drop `work_orders.booking_id` · 10 verify all projection consumers. |
| **Security hardening — RLS + PIN + roles ✅ (July 2, 2026)** | **Enforced Row-Level Security on every table** (keyed on `user_profiles.role` via `auth.uid()`; `get_my_role()`/`get_my_profile_id()` `SECURITY DEFINER` helpers; ~32 tables, each guarded with a `pg_tables` IF-EXISTS block). **PIN login** (numpad primary, email fallback; `staff_pins` + `verify_staff_pin` RPC + `/api/auth/pin`). First-login **SOP gate** (`SopGate` + `acknowledge_sop()` RPC). **RLS-enforced task visibility** (`dashboard_tasks.is_private` — Eli's self-tasks owner-only). Distinct **`tech`** role: nav hides CRM, Admin limited to Ops Log/Flags/Mics, **read-only `WorkOrderPopup`**, read-only bookings/clients for Calendar. **Private `checklist-photos`** bucket + signed URLs. Server-side **registration** (`/api/register`) + **inquiry** (`/api/inquiry`) routes. **Per-IP rate limiting** (`api_rate_limits` + `lib/rateLimit.ts`): `/inquiry` 3/min, `/api/ocr-receipt` 10/min + Bearer auth. Backup → service-role key. (Migrations `20260702*`; commits `c24e4de`→`10e6957`.) |
| **Perf + SOP + CRM ✅ (July 8, 2026)** | **Faster PIN login** — `signInWithPassword` (dedicated anon-key client) → browser `setSession`, single round-trip, replaces `generateLink`/`verifyOtp` (~3–4s delay removed); adds `staff_pins.supabase_password` (migration `20260708120000_staff_pins_supabase_password.sql`) + one-time `scripts/set-staff-passwords.mjs`; PIN bcrypt cost lowered 10→8 (git-ignored `.local.sql`). **PIN numpad keyboard input** (type or click). **SOP** — left sidebar nav (CRM · Clients · Tasks · Flags) + new Tasks/Flags sections + BOOKED copy rewrite (`public/sop.html`). **Temporary** CRM "Confirm Client Account" flow (renames Start Booking; modal-only, no redirect; sets `status:'booked'`). **Resolved July 28, 2026 (v1.1.1):** Start Booking was restored and now opens the WO; the confirm-client modal was **kept** but is reached only from the status pill, because marking a lead Booked is a distinct act from booking a session. Commits `1844257`→`af83cce`. |
| **Chunks 1–4 complete ✅** | **CRM + Clients — production-ready** |
| 1–3 | CRM core: Needs Action (Hot/Warm/Cold/Uncontacted tabs), All Leads with date separators + filters, touch logging, Keep Hot/Warm timers, Park feature, auto-demotion via Vercel Cron |
| 4.3 | Clients list page: two-column layout, filter chips, search, sort, pagination |
| 4.4 | Client detail panel: inline editing for label + COD views, contacts (A&Rs), artist chips, booking history, notes |
| 4.5 | Book Client modal: three-path flow (new/existing/label), registration token generation, `ContactPicker` + `ArtistPicker` reusable components |
| 4.6 | Public registration form at `/register/[token]`: token validation, all fields + inline validation, ID upload to Supabase Storage, client creation + lead backfill, route group isolation |
| 4.6b | Registration form improvements: iOS camera capture, scrollable T&Cs with iOS Safari overflow fix |
| 4.7 | Polish: registration QC notification banner on Clients page, empty states, confirm dialogs, mobile responsiveness |
| 4.9 | Detail card redesign: 2-col Contact + Session grids, cascading Location/Studio dropdowns, 12h TimeInput, editable Last Contact, session date picker, pills inline with name, Clients nav badge for pending registrations, Needs Action daily reset cron |
| **Chunk 6 calendar polish ✅** | **View switching, zoom, StudioView, scroll correctness** |
| 6-polish | Dynamic colW per view (week/2wks/month show correct date ranges), view-switch snaps to current Sunday, rAF shiftingRef guard fixes scroll race on column-width change, useLayoutEffect for initial grid measurement, StudioView blocks styled to match main calendar (black bg + status top bar, never truncates), zoom floor = Fit (removed 44px level), no post-scroll snapping |
| **CRM & Booking polish ✅** | **StudioSelect, label roster, A&R Admin, booking form improvements** |
| crm-polish-1 | New lead form: Source + Studio/Location dropdowns (replacing free text), Notes placeholder, COD mode hides Company/Label fields |
| crm-polish-2 | `lib/studios.ts` + `StudioSelect` component (flat "Venue · Studio" dropdown); `rate_daily` toggle on CRM and booking forms; booking form wired to StudioSelect + lead pre-fill |
| crm-polish-3 | `lib/roster.ts` — shared write gateway for `clients.artists[]`; roster-backed A&R + artist dropdowns on lead form, booking form, and client profile; Artists roster section on client profiles |
| crm-polish-4 | StudioSelect redesigned as true flat dropdown; lead form field order; "Move to Booking" navigates to `/calendar` |
| anr-admin-d1 | Admins section on label client profiles; `bookings.anr_contact_id` + `anr_admin_contact_id` FK columns added to schema |
| anr-admin-d2 | Admin dropdown in booking form; FK IDs saved on every booking write; contact popovers (A&R + Admin names → email/phone + action links) |
| anr-admin-d2b | Label booking card field order: Artist → A&R → Admin; name-as-popover-trigger; artist tiles in A&R card headers |
| label-card-inline | Inline A&R/Admin email/phone with Email/Call/Text buttons in booking card; underline `hasInfo` indicator; remove Edit/× from card header |
| **Registration status + view ✅** | **3-state reg button, view modal, PDF print route, multiple bug fixes** |
| chunk-crm-reg-view | 3-state registration button (Send Reg / Reg Sent / ✓ Registered); three-step token lookup on lead open; `generateRegLink()` stores `client_id` on token; `refreshRegStatus()` detects completion on Reg Sent click without page refresh; reg button hidden for Label/Billing leads |
| reg-view-modal | `RegViewModal` shared component (CRM + Clients profile); full registration record display with ID photo + Export PDF; `/register/view/[clientId]` print route with Paramount header |
| reg-fixes | 4 bug fixes: reg staying on Send Reg after completion, Use & Link not reflecting status, leads with client_id showing wrong state, re-query on click for real-time update |
| **Contact action buttons ✅** | **Call/Text/Email `<a>` links on all contact surfaces** |
| contact-actions | Lead detail card, COD client profile, A&R card headers, Admin card headers — all surfaces now have inline tel:/sms:/mailto: action links when field has a value |
| **SOP / Training tab ✅** | **Static training guide in nav** |
| sop-tab | `/sop` route + iframe; `public/sop.html` served statically; replace file to update guide with no code changes |
| **Global select styling ✅** | **`appearance: none` on all select elements** |
| select-styling | `styles/globals.css` global select rule strips native OS chrome so inline styles fully control appearance across all browsers and views |
| **WO save/sync overhaul ✅** | **Close & Save writes to work_orders + bookings; onSaved refetches + reopens form** |
| wo-sync | `handleClose` writes synced fields to both `work_orders` and `bookings`; `onSaved` prop refetches booking by ID and reopens form with fresh data; `initWO` query fixed from `.maybeSingle()` to `.limit(1)` (was creating hundreds of duplicate WOs); `liveForm` memoized to prevent spurious remounts |
| wo-time-table | FROM/TO removed from WO meta grid (redundant); studio time rows use `TimeInput`; single-day sessions seed stRow times from `liveForm` on open |
| wo-print | `@media print` CSS overhauled: centered full-width, `@page` 0.5cm margins, no scale transform, signature section stays on page; PDF filename via `document.title` |
| **Booking form polish ✅** | **Engineer edit-in-place, TBD button, multi-day label** |
| booking-polish | Engineer name clickable to reopen search pre-filled (ref-based to avoid stale closures); TBD button grey until active; multi-day sessions show "Edit times in WO" instead of FROM/TO inputs |
| **WO & daily ops amendment ✅** | **Runner Finish flow, NA photos, approval loop, Ops Log** |
| wo-daily-ops-amendment | `runner_finished` + `admin_approved` fields on `work_orders`; runner Finish button + confirmation dialog (WO stays editable after finish); NA notes + photo upload to `checklist-photos`; admin NA thumbnails in WorkOrderPopup; admin approve WO inline from LocationStrip drawer; WO z-index raised to 10010 above drawer |
| daily-ops-log | `/daily-ops-log` route + `DailyOpsLogSection` component; embedded as Ops Log tab in Admin sidebar alongside Engineers and SRS Log; shows approved WOs + ops submissions; click WO → WorkOrderPopup; click ops → DailyOpsModal |
| runner-post-amendment | UTC→local date fix on LocationStrip and all runner pages (`getLocalDateStr()`); runner WO footer redesigned (Cancel / Save / Finish three-button layout); NA photo `getPublicUrl()` + delete button; session card full-card tap target; approved WO 8am rule (stays in Today until operational day advances) |
| runner-session-info | Runner WO session info reads from live booking record (fetches booking after getting `booking_id` from WO); Label/A&R field shows `"LabelName / ContactName"`; artist card tap target; non-cancelled filter matches admin view |
| **Runner real-time ✅** | **`postgres_changes` subscriptions across all runner pages** |
| runner-realtime | `postgres_changes` on `bookings` + `work_orders` across runner hub, studio page, and WO page; session counts + WO badges update live; requires `ALTER PUBLICATION supabase_realtime ADD TABLE` + `REPLICA IDENTITY FULL` per table |
| **Day-rate WO ✅** | **Two-layout Studio Time table, charge fix, multi-day reconciliation** |
| day-rate-wo | Studio Time table has two layouts: day-rate (compact Date/Room/Hours/Rate/Charge) vs. hourly (multi-row time ranges); correct charge calculation; multi-day day-rate reconciles missing `studio_time_rows` on WO open; duplicate rows eliminated via upsert-by-sort_order |
| **Equipment Condition ✅** | **Horizontal scroll, sticky col, Not OK popup, PDF exclusion** |
| equip-condition | Equipment Condition table: horizontal scroll + sticky first column; Not OK cell opens inline popup for notes + photo upload; section excluded from WO PDF print |
| **WO live sync fixes ✅** | **Stale reads, form revert, post-save rate sync** |
| wo-live-sync | `isDayRate` reads from `liveForm` not stale `booking.*`; `wo?.id` added to reactive effect dep arrays so they re-run after `initWO`; post-save rate/date sync runs on every booking save (not day-rate only); `onSaved={undefined}` stops WO Close & Save from remounting booking form (preserves unsaved edits) |
| **Real-time project standard ✅** | **All four surfaces: runner WO stRows, admin WO popup, LocationStrip, calendar** |
| realtime-project-wide | Runner WO: `studio_time_rows` channel by WO id. Admin WorkOrderPopup: `studio_time_rows` + `work_orders` channels via `resolvedWoId` state. LocationStrip: `fetchDrawerData()` split from `openDrawer()` for silent refresh; `selectedLocRef` for callbacks. Calendar: `loadRef.current = load` pattern for stable subscription without re-subscribing |
| **Engineer in WO ✅** | **`bookings.engineer_rate`, eng sub-row in Studio Time, Engineer Total, runner compact table, auto-seed** |
| engineer-in-wo | `bookings.engineer_rate` text field (blank default); engineer sub-row in Studio Time on admin WO popup + runner WO page, both day-rate and hourly layouts; Col 1 blank, Col 2 eng name, eng_hours editable (rate locked on runner); `eng_charge = eng_hours × eng_rate`; Engineer Total in WO totals block; post-save rate sync from `bookings.engineer_rate` to `studio_time_rows.eng_rate`; runner compact table replaces stacked card view; auto-seed `studio_time_rows` from booking on WO open when none exist |
| **Ops Log search + nav cleanup ✅** | **Search bar in DailyOpsLogSection; Ops Log removed from top nav** |
| ops-log-search | Case-insensitive live filter in DailyOpsLogSection by client, artist, studio, engineer, invoice number; Ops Log nav item removed from top nav (accessible only from Admin sidebar tab) |
| **Runner session hero ✅** | **Artist name as primary headline on runner session cards** |
| runner-session-hero | Runner studio page session cards: `b.artist || b.client_name` as large hero; client name as smaller secondary text only when both present; matches admin SessionCard in LocationStrip drawer |
| **WO eng UX fixes ✅** | **eng hours default, $55 removed everywhere, date reconciliation for hourly, RT sync** |
| wo-eng-fixes | `normalizeStRow` defaults `eng_hours` from `total_hours` → `calcHours` when null; `eng_charge` recomputed on normalize when both `eng_hours` + `eng_rate` available; $55 default removed from booking form, runner WO, and admin WO `engRateDisplay`; Eng subtotal line added to Studio Time inline footer; live date range sync `useEffect` runs for both day-rate and hourly (no `isDayRate` guard); runner RT accepts admin-set `eng_hours` without overwriting runner-typed values; `bkData` fallback to URL `bookingId` param when `woData.booking_id` is null |
| **Per-row rate type + unified Studio Time table ✅** | **Architecture: each row toggles day/hour independently; single 9-col table replaces dual layouts** |
| per-row-rate-type | `studio_time_rows.row_rate_type` + `row_rate_daily` columns; `toggleRowRateType()` converts rate on toggle; booking-level rate-sync effects deleted; `normalizeStRow` branches on `row_rate_type`; unified Date\|SessionInfo\|From\|To\|Hrs\|Type\|Rate\|OT\|Total table in admin WorkOrderPopup + runner WO page; Type cell has Day/Hr inline toggle (admin) or display label (runner); `TimeInput` changed to 30-min `<select>` with 48 options; OT rate auto-populates from hourly rate; `shortDate()` helper for `M-D` date format; admin cell dividers removed; runner compact 444px table |
| **Runner WO UX polish ✅** | **Notes bottom sheet, iOS Safari scroll lock, viewport fixes, eng initials popover, PDF session notes** |
| runner-wo-ux | Notes bottom sheet: `position: fixed` floating card (`bottom:16, left:12, right:12, borderRadius:12`); iOS scroll lock via `body.position=fixed + top=-scrollY` pattern (not `overflow:hidden`); `Viewport` export in `app/layout.tsx` sets `maximumScale:1, userScalable:false`; runner root containers `maxWidth:100vw, overflowX:hidden`; eng name → initials pill with tap-to-expand fixed popover; `<span data-si-print>` reveals session notes in `@media print`; admin session info cell opens 280px popover with editable textarea |
| **Studio Time bugfixes ✅** | **Post-unification fixes: OT auto-calc, native date picker overlay, insert fixes, session notes restored, mobile column widths** |
| studio-time-fixes | 12-col admin / 11-col runner tables finalized; Session Info column restored (admin popover); OT auto-calc from `max(0,hours-12)`; OT Rate auto-seeded; new row auto-populates from previous; date cell → transparent `<input type="date">` overlay with auto-save + auto-sort on pick; all `upsert(onConflict)` → `insert()` (constraint never existed); removed invalid `booking_id` from runner insert; stripped `$`/`,` from `ot_rate` before write; `wo?.id` removed from date-range sync effect deps to fix initWO race; runner mobile column widths corrected |
| **WO status cycling ✅** | **`studio_time_rows.status`: in_progress → submitted → approved; runner Finish and admin Approve scoped to today's rows; status dots in Date cell** |
| wo-status-cycling | `status text NOT NULL DEFAULT 'in_progress' CHECK (status IN ('in_progress','submitted','approved'))` column on `studio_time_rows`; runner Finish submits today's rows; admin Approve approves today's rows; orange dot `#fb923c` = submitted, lime dot `#c8f04e` = approved in Date cell top-right; all insert sites seed `status:'in_progress'`; dots render based on `r.status` for all rows regardless of date |
| **Confirmed sessions + multi-day ✅** | **Daily Ops + runner hub filter to confirmed only; lte/gte date range for multi-day; badge driven by stRow status; RT channel for stRows** |
| confirmed-multiday | `.eq('status','confirmed')` on all booking queries; `.lte('start_date',today).gte('end_date',today)` for multi-day support; `pendingCount` badge derived from `studio_time_rows.status='submitted'` cross-referenced to WO ids; third LocationStrip RT channel on `studio_time_rows` UPDATE; approved sessions drop from Today drawer column |
| **WO Hub + WO status simplification ✅** | **`/wo-hub` route; WO status is `open`/`completed` only** |
| wo-hub | New `/wo-hub` page listing all WOs filterable by studio/date/status; WO status simplified to `open`/`completed`; "Complete WO" button toggles without locking popup; runner_finished flow removed (stRow status is the granular mechanism) |
| **Studio Time local-first ✅** | **All edits held in state, single DB commit on Save; Cancel fully reverts** |
| studio-time-local-first | All Studio Time cell edits (times, rate, type, date, add/delete row, eng clear) queued in React state; written to DB in batch on Close & Save; Cancel deletes pending inserts + restores pending deletes; WorkOrderPopup real-time subscription on `studio_time_rows` removed while popup is open |
| **`eng_visible` + `admin_locked` columns ✅** | **Eng sub-row visibility + row lock state persisted to DB** |
| eng-visible | `studio_time_rows.eng_visible boolean DEFAULT false` and `admin_locked boolean DEFAULT false`; eng sub-row shows/hides based on persisted DB value; locked rows read-only for runner; replaces ephemeral `autoEngRows`/`clearedEngRows` React state |
| **TimeInput rewrite ✅** | **Smart-parse text input replaces 30-min select** |
| timeinput-rewrite | `components/shared/TimeInput.tsx` rewritten to smart-parse text `<input>`; `parseTime()` handles AM/PM suffix, 24h numeric, colon format, already-normalized values; reverted from the June 5 `<select>` for mobile usability |
| **Runner WO bottom sections ✅** | **Rebuilt notes, equip, expenses; Session QC removed from nav** |
| runner-wo-bottom | Notes floating card (`position:fixed, bottom:16`); equipment horizontal scroll + sticky col; expenses inline add/remove; Session QC nav item removed |
| **Canvas signature pad + payment improvements ✅** | **COD-only legal section; payment type dropdown + memo + last_four; currency auto-format** |
| canvas-signature | `work_orders.print_name` + `work_orders.signature_data` (base64 PNG) replace `legal_signature/legal_name/legal_date`; COD-only guard; `touchAction:none` on canvas; `initialSigRef` pattern for reloading existing sig; both admin WO + runner WO aligned |
| payment-improvements | Payment type dropdown (Cash/Zelle/Credit Card/Debit Card/Check/Other); `memo` text field; `last_four` text field (Credit/Debit only); `× remove` per row; runner payment section now editable; `formatCurrency`/`stripCurrency` helpers; `$`/`,` stripped before DB write |
| **Mic Inventory UI ✅** | **Runner mic inventory page with collapsible sections, Here/Room/Missing status, qty steppers, submit flow** |
| mic-inventory-ui | `/runner/[studio]/mics` page built; collapsible sections per mic category; Here/Room/Missing condition tracking; qty steppers; submit flow; appears in Yesterday checklists (`liveDoc: false` fix) |
| **dashboard_tasks table ✅** | **Supabase migration: per-role task system table** |
| dashboard-tasks-migration | `dashboard_tasks` table: `assigned_role` CHECK ('admin','studio_manager','asst_manager','billing'), `source` CHECK ('manual','runner_flag','wo_flag'), `photo_url text`, soft delete via `deleted_at`, `set_updated_at()` trigger, RLS enabled with placeholder `USING (true)` on INSERT/UPDATE/DELETE until Chunk 9 auth; SELECT filters `deleted_at IS NULL`; GRANT to `authenticated` + `anon` (anon grant added Session 3a — app uses anon key pre-auth) |
| **Dashboard rebuild ✅** | **3-column dashboard layout: Needs Action, Today's Sessions, Tasks placeholder** |
| dashboard-rebuild | `app/(main)/page.tsx` rewritten; `TodoModule` + `QCHomeWidget` removed; 3-col grid (`1fr 2fr 1fr`). Col 1: Needs Action — top 5 hot/warm/uncontacted leads (`needs_contact=true`, excludes cold/dead/booked), status badge, "View all in CRM →". Col 2: Today's Sessions — confirmed (#14B8A6) + tentative (#F97316) sections with colored left-border rows, `b.artist\|\|b.client_name`, session_type badge, time + location. Col 3: Tasks placeholder (replaced by live Tasks panel in Session 3a). Cold leads exclusion fix in follow-up commit. |
| **Dashboard Tasks panel ✅** | **Session 3a: live ticket-style task system wired to dashboard Col 3** |
| dashboard-tasks-session-3a | `dashboard_task_comments` table (per-task comment thread, append-only, anon+authenticated INSERT/SELECT). `dashboard_tasks.photo_url` column added. Tab→role mapping: Me=admin, Mgr=studio_manager, Asst=asst_manager, Billing=billing. Task list fetches by role, rows clickable. Inline add task form with optional photo (to `checklist-photos` bucket). Task modal: title, task photo, comment thread with photos + timestamps, textarea + attach photo, Comment + Complete buttons. `created_by_name` from `supabase.auth.getUser()`, falls back to `'Staff'`. `DashboardTaskComment` type + `DashboardTask.photo_url` added to `lib/supabase.ts`. |
| **Yesterday checklist rows ✅** | **Color-coded state, approve flow, 8am reset** |
| yesterday-checklist-rows | LocationStrip drawer Yesterday ops rows always render (grey when no submission). Color states: green=approved, orange=submitted, grey=in-progress. Approve button on each row updates `admin_approved_at` + removes row from Yesterday column immediately. |
| **Dashboard Tasks panel polish ✅** | **Card-row layout, count badge, history modal, runner flag accent** |
| dashboard-tasks-polish | Task rows restyled as cards with border + source pills. Count badge on tab header. "X completed →" history link opens completed-tasks modal with search. History rows clickable → read-only ticket modal. Runner flag orange left-border accent on auto-generated tasks. Dot colors: orange = open, teal = completed in history. |
| **Runner quick action card submitted state ✅** | **Green border when submitted today; fixed submitted state queries** |
| runner-quick-action-state | Quick action tiles on runner studio hub show green `#4ef0a2` left border when submitted today. Submitted state queries both `checklists.completed_at` (checklist types) and `daily_ops_submissions.submitted_at` (all categories). Stock page now writes to `daily_ops_submissions` on save. All runner pages auto-navigate to hub after Save or Submit. |
| **Runner Save + Submit pattern ✅** | **Save preserves progress; Submit marks complete; initials required hint** |
| runner-save-submit | Checklist and mics pages: Save persists to `checklists`/`mic_checkins`/`mic_inventory_quantities` without writing `submitted_at`; Submit writes `submitted_at` to `daily_ops_submissions`. Mics page restores prior checkin/quantity from DB on load. Red "Required to submit" hint anchored below initials input on Submit with empty field. |
| **Runner daily ops submission fixes ✅** | **Local date, OPS_CATS key, immutable save, column schema** |
| runner-daily-ops-fixes | UTC→local date fix on checklist, stock, petty-cash `daily_ops_submissions` writes. LocationStrip OPS_CATS `'stock_list'` → `'stock'` key fix. Immutable save pattern for stock and petty-cash (eliminates duplicate inserts from direct React state mutation). Removed non-existent columns from checklist `daily_ops_submissions` upsert that were causing silent 400 errors. |
| **Daily ops Today/Yesterday view fixes ✅** | **Approved items and completed WOs no longer disappear from wrong column** |
| daily-ops-view-fixes | Today column no longer filters out approved ops rows (approval doesn't hide them until the day advances). Yesterday column no longer filters out completed WOs — `pastRetentionWindow` guard scoped to Today's `activeTodayBkgs` only. |
| **Petty cash running ledger ✅** | **All-time ledger, most-recent balance, In/Out toggle, admin always visible** |
| petty-cash-ledger | Loads all prior entries for the studio (not just today); most-recent `petty_cash_balances` row for opening balance; In/Out tap-to-toggle replaces `<select>`; save errors surfaced inline; admin balance view unblocked from runner submission state. |
| **Daily Ops Log rebuilt ✅** | **Date-based historical view with studio tabs, status dots, day modal — replaces flat mixed table** |
| daily-ops-log-rebuild | Studio tabs (Paramount/Encore/Ameraycan/Track); date list sorted most-recent first with teal/amber/grey status dots; Load More (25 at a time); day modal shows WO cards + 5 checklist rows with Runner/Admin checkboxes; WO card click opens WorkOrderPopup. |
| **Flags system ✅** | **Structured issues log: dashboard panel, flag modal, acknowledge/resolve, runner/WO auto-flag, Admin log** |
| flags-system | `flags` table (id, studio, source manual/runner_flag/wo_flag, runner_note, category facility_general/gear_equipment/client_billing, status pending/acknowledged/resolved, deleted_at soft delete). `flag_comments` table (flag_id FK CASCADE, text, photo_url, created_by_name, created_at; append-only). `Flag` + `FlagComment` types in `lib/supabase.ts`. Dashboard panel: 4 cards, studio pill, category badge, runner note snippet, lime "View all flags →". Flag modal: comment thread, Acknowledge (+ category reassignment dropdown), Resolve sub-modal (resolution note / vendor / cost → appended as flag_comment + status=resolved). Soft delete with inline confirmation on card (×) and inside modal. Runner checklist NA submissions and WO NA submissions auto-insert flags with source=runner_flag/wo_flag respectively. Admin Flags Log tab (searchable by note/studio/category, per-row soft delete). |
| **Dashboard room grid ✅** | **Col 2 replaced with fixed 11-room grid and day navigation** |
| dashboard-room-grid | `ROOMS` constant (11 rooms: Paramount A/B/C/E/X, Ameraycan A/B, Encore A/B, Track North/South). `calDate` state + `‹`/`›` prev/next day buttons in panel header. `useEffect` dep `[calDate]`, bookings fetch uses `new Date(calDate)`. Grid: `gridTemplateColumns: '1fr 1fr 1fr'`, `gap: 4`, panel `height: 556`. Room match: `b.location === room.venue && b.studio === room.studio` (booking.location = bare venue name, not combined string). Booked card: teal `#14B8A6` or orange `#F97316` top border, DM Serif artist name (`var(--text)`), label sub-line (DM Mono, muted), compact time via `fmtSessionTime()`, `1ST-XX`/`2ND-XX` engineer+assistant initials bottom-right (teal=confirmed, amber=hold). Module-level helpers: `engInitials(name)` + `fmtSessionTime(t)`. |

| **Dashboard room grid → booking modal ✅** | **Booked cards open the booking form from the dashboard** |
| dashboard-room-grid-modal | Booked cards in the dashboard 11-room grid are clickable → open the booking form modal directly. Nav z-index reaffirmed (99999) above the modal. |
| **Booking form artist search + A&R autoselect ✅** | **Search matches artist names; picking one auto-resolves the A&R** |
| booking-artist-search | Client search returns client/label name, A&R name, and artist-name matches. Artist pick auto-selects the A&R, filling `ordered_by`/`anr_contact_id`/email/phone. `ClientProfile.saveContact` strips `id`+`client_id` from the `client_contacts` update payload. **Note:** the artist-match source and A&R filter were reworked the same day — see `artist-search-thru-contacts` below. |
| **UnifiedSessionForm (experimental) 🧪** | **Parallel single-surface session form; owner-gated** |
| unified-session-form | `components/unified/UnifiedSessionForm.tsx` combines client card + Work Order in one natural-flow modal (header → status chips → client card → work order). Renders the real `WorkOrderPopup` inline via a new `inline?: boolean` prop (normal flow, no fixed overlay/backdrop). Launched only from a temp "⚡ USF" Nav button shown when `localStorage.userRole === 'owner'` (no auth yet — Chunk 9). Does NOT replace the production `BookingForm` + portaled `WorkOrderPopup`; both coexist. First scaffold reverted, rebuilt as v2 against the real WorkOrderPopup. |
| **WO → Calendar sync ✅** | **Schedule round-trips WO↔booking; WO owns dates/times once it has real data** |
| wo-calendar-sync | WO Close & Save (`WorkOrderPopup.handleClose`) syncs `start_date`/`end_date` (earliest/latest dated `studio_time_row`) + `from_time`/`to_time` (earliest dated row) back to `bookings`. Calendar refetches via `onSaved={() => { loadRef.current(); setReloadKey(k+1) }}` (2wk/week grid reads `bookings`; DayView/StudioView read `reloadKey`). **Bugfix:** stopped syncing `studio` — `studio_time_rows.studio` is a bare letter (`'A'`) while the calendar filters full labels (`'Studio A'`), so synced bookings matched no room and blocks vanished (`3361fdb`); also removed stray `WorkOrderPopup 2.tsx`. **Ownership gating (`ce3194d`):** `calendar/page.tsx handleSave` strips the four schedule fields from the `bookings` update and skips day-rate stRow reconciliation when the booking's WO has ≥1 dated row (`woOwnsSchedule`) — the WO becomes the sole authoritative schedule writer; no-WO/empty-WO bookings still own + bootstrap. |
| **A&R artist persistence + artist-search rework ✅** | **Found the real persistence bug; routed artist search through A&R contacts** |
| ar-artist-persistence | **(`af957e8`)** A&R `client_contacts.artists[]` weren't persisting. Real cause: `app/(main)/clients/page.tsx` `load()` refetched `client_contacts` without `artists`/`contact_type`, so `onRefresh()` reset `ContactRow.localArtists` to `[]` (UI reverted) and the next save wrote `[]` back (DB wiped). Fix: added `artists, contact_type` to the select. DB write path was always healthy (direct PATCH persists, 204). Also repaired the A&R/Admin filter split that depends on `contact_type`. Corrects the earlier "strip `id`/`client_id` fixed the save" framing — the strip is correct practice but was not the persistence fix. |
| artist-search-thru-contacts | **(`d7c8e82`)** Booking-form artist search reworked: the third parallel query now fetches `client_contacts` (each A&R's own `artists[]` + `contact_type` + joined parent `clients`), pre-filtered `.neq('artists','{}')`, matched client-side — instead of scanning the label record's `clients.artists[]`. Result `record` is the parent client + `_artistMatch`; admins excluded via `ct.contact_type === 'admin'` continue (null-type kept). The `_artistMatch` autofill re-query changed from `.neq('contact_type','admin')` to `.or('contact_type.eq.anr,contact_type.is.null')` because PostgREST `.neq` drops NULL-type rows (most A&Rs). |
| **Admin Mic Inventory tab ✅** | **Read-only consolidated cross-studio mic view with missing-mic alerts** |
| admin-mic-inventory | **(`8529b8b`, `eefd6c1`)** `components/admin/MicInventorySection.tsx` added as a fifth Admin sidebar tab (`mic_inventory`). Full-width dismissable missing-mic banner (every mic whose latest checkin across any studio is `missing`). Horizontal studio tabs (Paramount/Ameraycan/Encore/Track/Floating Gear) with mic count + red missing badge + active-tab color underline. Table: Mic Name \| Status \| Room \| Qty \| Last Submitted By \| Date; Here `#14B8A6` / Room `#F97316` / Missing `#ef4444`; missing rows red left border + tint; sort missing→room→here→none then `sort_order`. Per-tab Show History (last 7 checkin nights per mic). Reads `mics`/`mic_checkins`/`mic_inventory_quantities`/`mic_inventory_submissions`, reduced client-side to latest-per-(studio,mic); studio tabs resolve by home studio, Floating + banner resolve across any studio. No new tables, read-only. Tab started as collapsible vertical sections (`8529b8b`), refactored to horizontal tabs (`eefd6c1`). |
| **Admin Mic Inventory editing ✅** | **Inline-cell status editing + Manage Mics modal; `mic_checkins.source`/`amended_by`** |
| admin-mic-editing | **(`6dfb4d6`, `e8d234c`, `b091758`, `fdecc26`, `65d2cda`)** Migration: `mic_checkins` + `source text NOT NULL DEFAULT 'runner'`, `amended_by text`. Per-row `✎` turns the row's own cells into in-place inputs (single shared `GRID_COLS` so they align under headers; lime tint on the editing row): Status dropdown, per-studio Room dropdown (`STUDIO_ROOMS`, status=Room only), Qty numeric, Last-Submitted-By with greyed "Initials" placeholder. Save upserts `mic_checkins` (`onConflict mic_id,studio,date`, overwrites that day's row) with `source:'admin'`/`amended_by`; changed Qty also upserts `mic_inventory_quantities`. Teal ADMIN badge on admin-sourced rows (main/history/banner). Manage Mics modal: Master Mic List (search, inline edit, deactivate/reactivate — qty→`mic_inventory_quantities`) + Add Mic (→ `mics`, `sort_order=max+1`). Component now loads ALL mics, derives active-only for display, reloads after every write. |
| **Engineers table actions fix ✅** | **Edit/Deactivate no longer overlap the Status pill** |
| engineers-actions-col | **(`898ecb0`)** Admin Engineers table actions column was 80px but holds Edit + Deactivate (~150px); `flex-end` overflow spilled left over the 80px Status pill. Widened the actions column 80px→180px in both header and row grid templates. |
| **UI polish pass ✅** | **Shared StatusBadge + SectionHeader primitives, nav tab underline, room-grid/chip glow** |
| ui-polish | **(`052819d`, `288947b`, `62ad623`, `d628449`, `1810e53`)** `components/ui/StatusBadge.tsx` + `components/ui/SectionHeader.tsx` replace ad-hoc status text and section headings across dashboard, CRM, admin, wo-hub, flags, and WorkOrderPopup (CRM's local `SectionHeader` field-label helper renamed `FieldGroupLabel`). Nav tabs restyled to a bottom-border underline (active 2px `#c8f04e` + `#e8eaf0`, inactive `#6B7280`/hover `#9ca3af`, full-height DM Mono 11px tabs); calendar tab count badge removed (`tentativeCount` state/fetch kept). Dashboard room-grid cards get teal/orange state glow + 2px top bar; calendar booking chips recolored confirmed `#22c55e`→`#14B8A6` (all three `STATUS_TOP_COLORS` maps) + subtle glow on BookingBlock/DayView/StudioView — "open-WO" orange maps to the existing `tentative` signal (no WO data fetched on those surfaces). CRM lead-row card redesign (`f934716`) was reverted (`3df8016`), not shipped. |

| **Auth — login + route protection ✅** | **Supabase Auth login, forgot/reset password, client-side guard** |
| auth-login | `app/(auth)/login` (`signInWithPassword` → `/`; `resetPasswordForEmail`) + `app/(auth)/reset-password` (`updateUser`); `components/auth/AuthGuard.tsx` wraps `(main)` layout (checks `getSession`, redirects to `/login`, `onAuthStateChange`); login bounces authed users to `/`. Client guard (no `@supabase/ssr`/middleware; localStorage sessions); `(main)` routes only — runner/register/auth public. Nav Sign Out button. (Was UX gating, RLS off — **RLS now enforces data security as of July 2, 2026;** PIN login added.) Also: nav clock upgraded (12px/`#e8eaf0`/500) + redundant dashboard date/time block removed. |
| **Schema — user_profiles + task assignment ✅** | **`user_profiles` table + `dashboard_tasks.assigned_to`/`assigned_by`** |
| user-profiles | `supabase/user_profiles.sql` (surrogate `id` PK, nullable `auth_user_id` FK→auth.users, `email` unique lookup key, role `owner/manager/billing/asst_manager/tech`, soft delete; seed 6→8 staff; run manually in SQL editor — Claude has no DDL access). `supabase/dashboard-tasks-assignment.sql` adds `assigned_to`/`assigned_by` uuid FK→`user_profiles.id`. Role set + roster changed in the live DB after the migration. |
| **Personalized greeting + task panel rebuild ✅** | **`useUserProfile` hook; 6 per-user task tabs; add-task + detail modals** |
| task-panel-rebuild | `hooks/useUserProfile.ts` (resolve profile by session email); greeting shows `display_name`. Dashboard task tabs rebuilt to 6 per-user tabs (Eli/Adam-Mike/Fernando/Aaron/Asst Mgr/Tech) resolved by display_name (no hardcoded UUIDs), driven by `assigned_to`; visibility by role (owner/manager/billing all; asst_manager→Asst Mgr; tech→Tech); horizontally scrollable tab bar (`.hide-scrollbar`). Full add-task modal with flat Assign-to dropdown (Asst Mgr→Quinn's id, Tech→Sierra's id; owner/manager/billing only). Task detail modal redesigned (Complete header button; description + assigned meta + UPDATES thread + comment Submit; footer Delete[canAssign]/Cancel/Save&Close); task rows single-line truncated. `assigned_role` vestigial (new tasks set `'admin'`). |
| **Add-task photo ✅** | **Resolved June 26 — not a defect; previews added for clarity** |
| add-task-photo | `dashboard_tasks.photo_url` exists, insert persists it, `uploadPhoto` returns a valid URL — the NULL rows were test tasks saved with no photo attached, not a bug. Thumbnail preview + `.select()` insert-error logging in the add-task modal (`056aa79`); same preview added to the task-detail **comment** section (`6211d17`). Closed — no bucket-policy/sanitization change needed. |
| **Task comment photo preview ✅** | **`<img>` thumbnail on file-pick in the task-detail comment section** |
| task-comment-photo | **(`6211d17`)** Comment section in the task-detail modal shows an `<img>` thumbnail the moment a photo is picked (max-height 80, 4px radius, above "+ Attach photo"). `commentPhotoPreview` state + `pickCommentPhoto`/`clearCommentPhoto` (create+revoke object URL); all reset paths (open/comment/complete/cancel/save&close/delete) revoke via `clearCommentPhoto`. Mirrors the add-task modal. |
| **Task panel cap + `/tasks` page ✅** | **Dashboard panel caps at 9 + "show all tasks →"; full `/tasks` page; `lib/tasks.ts` extraction** |
| tasks-page | **(`f999fd4`)** Dashboard Tasks panel renders `tasks.slice(0,9)` + muted "+ N more" link; header link `history →` → `show all tasks →` (→ `/tasks`). New `app/(main)/tasks/page.tsx`: same 6 tabs + role visibility, Active + Completed count-pilled `SectionHeader` sections, wider rows with `assigned_by` + `created_at`, replicated task-detail modal; **not in nav** (dashboard-only). Shared task tab/roster helpers extracted to `lib/tasks.ts` (`TAB_DEFS`, `ASSIGN_OPTIONS`, `resolveAssignTo`, `nameForId`, `visibleTabsForRole`, `idsForTab`, `fetchTasks`, `fetchCompletedTasks`, `fmtTaskTime`, `uploadTaskPhoto`) and imported by both surfaces. Dashboard's old history modal now dead/unreachable, left in place. |
| **`/tasks` completed collapsed + search ✅** | **Completed collapsed by default with header toggle + search-overrides-collapse** |
| tasks-collapse-search | **(`e102447d`)** Completed section collapsed by default; header toggle renders `COMPLETED (n) ▼`/`▲`. Added a "Search tasks…" box filtering both sections (case-insensitive on `text`); a non-empty query overrides the collapse so search always reaches completed. Literal `(n)` format meant inlining the count in the title (no teal count pill on Completed). |
| **Dashboard lead → CRM deep-link ✅** | **Needs Action lead click opens the lead in CRM** |
| dashboard-lead-crm | **(`8260fd4`)** Needs Action lead rows click → `/crm?lead=<id>`. CRM reads the param in a mount effect via `window.location.search` (matching its `?clientId=`/`?id=` pattern — chosen over `useSearchParams` to avoid a page-level `<Suspense>` restructure the static build would require); opens the lead in the detail panel, switches off `analytics`, and sets `hasAutoSelected` so the default auto-select doesn't override it. |
| **Dashboard empty room → booking modal ✅** | **Empty room-grid card opens the calendar's new-booking form pre-filled** |
| dashboard-room-newbooking | **(`418ebfb`)** Empty room cards: pointer + lime hover border `rgba(200,240,78,0.2)` (no added text) → `/calendar?newBooking=1&location=&studio=&date=`. Calendar effect fires when `newBooking` is present without a `clientId` (no collision with Start Booking) and reuses the existing `openNew(location, studio, date)` — real `BookingForm` + `handleSave`, no duplication. Occupied cards stay clickable (kept the shipped open-booking behavior, declining the spec's "non-clickable"). |
| **Daily Google Drive backup ✅** | **GitHub Actions cron → Supabase REST → Drive via googleapis** |
| daily-backup | **(`295401f` + `2d78713`/`12bad5b`/`2623d88`/`d933fce`/`c18b983`/`a6a027f`/`ca8038a`)** `scripts/backup.mjs` + `.github/workflows/daily-backup.yml` (cron `0 8 * * *` + `workflow_dispatch`, Node 24). Backs up 17 tables to a Shared Drive folder via `googleapis` (`supportsAllDrives:true`), auth from `GOOGLE_SERVICE_ACCOUNT_JSON`. Uses Supabase **REST** with `fetch()` — not `@supabase/supabase-js`, whose realtime client crashes under Node 20+ without `ws`. Paginates 1000/page by `offset`; `deleted_at IS NULL` filter with auto-fallback for tables lacking the column. Table names verified against live `.from()` usage. Per-table logging; failed table skipped; `process.exit(1)` only on upload failure (404 → "share folder with service account email" + `client_email`). `googleapis` added to deps (lockfile updated for `npm ci`). |
| **Mobile responsiveness pass ✅** | **`useIsMobile` standard; dashboard, CRM, calendar, WO popup, booking form, nav all phone-usable; desktop unchanged** |
| mobile-pass | **(June 26 eve: `898f34c`/`a794517`/`d2bdedc`/`c6d7e51`/`8ecf9dc`/`8e2c25e`/`4120cda`/`b913832`/`41ea3cf`/`35af433`/`eb9011e`/`48b07a4`/`2eba354`/`f90ca0c`/`c51680a`/`ef284a3`/`0a047d1`/`3395f27`)** New `hooks/useIsMobile.ts` (768px, desktop-first) gates every change; `globals.css` adds the mobile gutter + overflow-x guard. **Dashboard:** 3-col → single column reordered (Sessions/Needs Action/Tasks via CSS `order`), smaller room cards, compact task rows, one-line hero, `fullscreenCardOnMobile()` for modals. **Nav:** 44px `≡` hamburger → full-width dropdown; STUDIO OS badge + clock + USF removed; logo links home. **CRM:** single-panel (list ↔ full-screen detail with "← Leads"), mobile auto-select suppressed. **Calendar:** defaults to Day view (responsive DayView — sidebar hidden, rooms single-column), two-row controls bar, full-width lime New Booking, 80px room labels, location codes PRS/ARS/ERS/TRS, month corner label, tappable date-input overlay, window-filtered chips; **no swipe** (tried + removed). **WO popup + BookingForm:** full-screen `100dvh` flex sheets; WO popup mirrors the Runner WO page (section cards, read-only SESSION INFO, META/branding hidden, two-button footer, Complete WO in body, horizontal-scroll tables); BookingForm fixed header/footer via `minHeight:0`. **Chips:** text recolored white/muted (`#e8eaf0`/`#9ca3af`/`#6B7280`) regardless of payment type. |
| **Welcome splash + dashboard hero clock ✅** | **One-time fresh-login splash with no-flash hold; live clock moved nav → hero** |
| welcome-splash-clock | **(`4c0d2a0`/`47f333f`/`dbf1229`/`372c7f4`/`08e57f3`/`ae908d5`/`8ade55f`/`45beda2`)** Login sets `sessionStorage 'showWelcome'`; dashboard shows a one-time full-screen splash (greeting + `display_name` + PRG footer, `zIndex 100000`), clears the flag on mount, dispatches `welcomeDone`. No-flash hold: `AuthGuard` renders a `#0d0f14` full-screen div while the session resolves when the flag is set; `NavGate` hides the nav until `welcomeDone` (3s fallback). The live clock was removed from the Nav and moved to the dashboard hero (desktop only, DM Serif Display, date `#c8f04e` + time `#e8eaf0` inline). |
| **USF removed from nav ✅** | **Experimental UnifiedSessionForm no longer launchable** |
| usf-removed | **(`9e65fcb`)** The owner-gated "⚡ USF" Nav button + `localStorage.userRole === 'owner'` gate removed from `Nav.tsx`. `components/unified/UnifiedSessionForm.tsx` and `WorkOrderPopup`'s `inline` prop remain in the repo but are now dead (nothing launches USF). Production flow is `BookingForm` + portaled `WorkOrderPopup`. |
| **Calendar inner-glow rework ✅** | **Booking chips glow inward like the dashboard; day-view studio name muted** |
| calendar-inner-glow | **(`893679c`/`965b8b4`/`cf7ca70`/`4fba14c`/`477fa9f`/`5f4e24c`/`8ed375c`)** All calendar booking chips (`BookingBlock` week/2-week grid, `StudioView` cells, `DayView` chips) switched from an outer `0 0 8px rgba(…,0.15)` halo to `inset 0 0 18px rgba(20,184,166,0.06)` (teal) / `rgba(249,115,22,0.06)` (orange) — the dashboard room-card value — so the glow diffuses inward over each chip's `#0d0f14` background instead of haloing into the surface. The Day-view *studio card* (opaque header + chips) can't show an inset shadow on itself, so its glow is painted on a transparent absolute overlay (`inset:0; pointerEvents:none; zIndex` above the chips); the card keeps a subtle `rgba(20,184,166,0.2)` border + 2px teal top bar as its container occupied-signal. Day-view studio-card name recolored lime `#c8f04e` → muted white `#9ca3af`. Supersedes the `0 0 8px` chip glow from the June 24 ui-polish row. |
| **Splash name fade-in ✅** | **Welcome-splash name animates in; smoother fade-out** |
| splash-name-fade | **(`0050c76`)** Welcome splash `display_name` fades in 300ms after mount via a `nameVisible` state: `opacity 0→1` + `translateY(8px→0)`, `transition: opacity 0.6s ease, transform 0.6s ease`. Greeting + "PARAMOUNT RECORDING GROUP" footer have no per-element animation, so they appear immediately with the splash. Whole-overlay fade-out `transition` bumped `0.5s`→`0.6s ease` — it's on the parent overlay so all children fade together and the name never exits before the rest. |
| **Lead-form universal client search ✅** | **NewLeadModal Label mode searches label + A&R + artist in one field, mirroring the booking form** |
| lead-universal-search | **(`535eaab`)** `app/(main)/crm/page.tsx` `NewLeadModal` Label mode now leads with one "Search client name…" field running three parallel queries — `clients` label names, `client_contacts` A&R names, `client_contacts.artists[]` artist names (matched client-side, admins excluded via `contact_type === 'admin'`) — the same combined-search pattern as `BookingForm`. Result rows show **Artist (bold) · Label · A&R**; picking one autofills + links LABEL (`labelClientId`, so Move-to-Booking still works), A&R/REP (`anr_contact_id` + email/phone), and ARTIST. The three fields below became plain **editable** inputs (LABEL no longer searches; A&R lost its "Select a label first" disable gate), so an un-matched lead can be typed by hand. Removed the dead label-only-search code (`selectLabelClient`, `handleLabelKeyDown`, `labelClientSuggestions`/`showLabelClientDD`/`labelHighlight`/`labelDebounce`/`skipLabelSearch`); added a `UniSuggestion` type. COD mode, the COD/Label toggle, and EMAIL/PHONE are untouched. |
| **PWA — installable icons + manifests ✅** | **Add-to-home-screen for the main app and a separate Runner hub app; icons + manifests only, no service worker** |
| pwa-icons-manifests | **(`fe17795`)** Makes PRSFlo installable to a phone home screen (branded icon, `display: standalone`). **No service worker / no offline caching** (deliberate; offline is separate future work). Two manifests + icon sets: `public/manifest.json` (`start_url "/"`) for the main app, `public/runner-manifest.json` (`start_url "/runner"`, `short_name "Runner"`) so runners install the Runner hub as its own app with a distinct icon; both `background_color`/`theme_color` `#0d0f14`, icons 192/512/512-maskable. Icons wired via the **Next.js Metadata API, not raw `<head>` tags** — root `app/layout.tsx` sets `metadata.manifest`/`metadata.icons` (+ `viewport.themeColor`), new server-component `app/runner/layout.tsx` overrides `manifest`/`icons.apple` + `appleWebApp` for the `/runner/*` subtree (proper parent→child override; raw global head tags would have leaked the main manifest/apple-icon onto `/runner` and duplicated). `scripts/generate-icons.js` rasterizes inline SVG sources via `sharp` (`^0.35.2` dev dep); re-run with `node scripts/generate-icons.js`. **NOTE: the original teal-`RUNNER`-badged text icons here were replaced by the wave design — see the icon-redesign row below.** iOS "Add to Home Screen" still needs a real-device check. |
| **PRSFlo rebrand + wave icon ✅** | **PRSFlow → PRSFlo display name; no-text wave mark replaces the text icons; locked wordmark** |
| prsflo-rebrand-wave | **(`0680a84`/`f1fc649`/`bf6afb4`/`6ceea3b`/`1ee573d`/`89c3ea3`)** Renamed the product **PRSFlow → PRSFlo** in UI display, metadata (`title`, both manifests' `name`/`short_name`), and doc prose; **out of scope** (left "prsflow"): repo, folder, Vercel project, domain `prsflow.paramountrecording.com`, Supabase, env vars, `package.json` name, backup filename, the `PRSFlow-Tech-Stack.md` filename. App/icon art replaced with a **no-text wave mark** — three stacked sine waves (teal-gradient `#5DCAA5→#0e5446` @0.6, lime-gradient `#e3f99c→#8ab030` @0.9, white `#e8eaf0`) + drop shadow, on a **radial-gradient** background (`#1a1d24`→`#0a0b0e`). `generate-icons.js` rewritten → 5 main PNGs + `icon.svg`, 3 runner PNGs + `runner-icon.svg`; the **runner set swaps the teal top wave for orange** (`#fbb86c→#c2540a`). New `components/PRSFloIcon.tsx` = the bare wave mark (no square) with a `size` prop + a centered teal radial glow (1.4× size); used in the nav (38), login/reset/splash (72), runner hub (32). **Locked wordmark rule:** `Nav.tsx` is the single source of truth for PRS/Flo span styling — `fontFamily 'Syne'`, weight 800/500, `PRS var(--accent)` / `Flo var(--text)` @0.45; copy exactly, never recreate (documented in CLAUDE.md → Locked Design Conventions). |
| **CRM LeadDetail redesign ✅** | **Unified identity lockup, inline status strip, meta row, inline reg button, phone auto-format** |
| crm-leaddetail-redesign | **(`243a884`/`47d76b0`/`a974724`/`5a127c1`/`d595098`/`6a28a66`/`4802276`)** Rebuilt the CRM lead detail (`app/(main)/crm/page.tsx`): COD `fname/lname` hero or Label `Label — Artist` hero + A&R sub-line; one compact contact line (email/phone + action buttons), no field labels/`Created`. The bordered status box → a borderless inline strip (heat `<select>`, `● Needs Contact`, far-right teal `Keep hot/warm until …`) + a 1px `#1e2028` divider; pills moved to a muted bottom **meta row** (`BILLING · RECORDING SESSION · INSTAGRAM`). Send Reg button moved inline into the hero row + the reg-link panel relocated directly below it. `fmtPhone()` auto-formats the phone (display-on-blur); email left-aligned. Default view → **All Leads**; Needs-Action and status-strip Keep-Hot/Warm **labels** now key off `lead.status` (not the bucket tab). |
| **CRM/Clients lime → outlined-dark buttons ✅** | **Action buttons converted from lime fill to transparent + white-text + border; COD/Label color retired** |
| crm-clients-button-pass | **(`d8c6b67`→`3e3d3e4`/`16cd055`/`f5136d1`/`a4495bd`/`cdd5234`/`9accc20`)** Start Booking, + New Lead, + New Client, CRM Contact buttons, and the ClientProfile Add/Save/Add-Contact/Add-Admin buttons flipped from `var(--accent)` lime fill to the **outlined dark pill** (`transparent` bg, `#e8eaf0` text, `1px solid var(--border)`) — done wrong (white fill) once, then corrected to the outline. ClientProfile lost its standalone label-roster "Artists" section (per-A&R chips/editor + roster sync stay) and the dead roster state/import; A&R cards now show the phone number text. The **COD/Label blue/lavender** convention (`#7BBFFF`/`#96A9FF`) was retired from the CRM lead detail + booking form + WO modal (`leadNameColor()` → `var(--text)`; dead `COLOR_COD`/`COLOR_LABEL` removed). Heat-tied controls keep their status colors. |
| **CRM New Lead client_id + Dashboard Needs Action ✅** | **★ Client match persists client_id on any temperature; dashboard filter mirrors CRM buckets** |
| crm-clientid-dashboard-na | **(`d58e99b`/`02bbfe4`/`0b369b6`)** New Lead modal now persists `client_id` whenever a ★ Client match is selected (was gated to `temperature === 'booking'`, so Hot/Warm matches lost the link and the reg state couldn't inherit). The dashboard Needs Action panel was re-aligned to the CRM's exact bucket predicates (`isKhuDue`/`isParked`/`getMissing`, all gated on `needs_contact !== false`), sorted by `updated_at` desc, capped at 6 — required adding `updated_at: string | null` to the `Lead` type. |
| **WO day-rate seed fix + DayView notes ✅** | **All studio_time_rows seed paths write row_rate_type/rate_daily; runner WO day rates compute correctly** |
| wo-dayrate-seed-fix | **(`dda16b3`/`cfef95c`/`f8558a0`)** Every admin + runner `studio_time_rows` seed/insert path now writes `row_rate_type` + `rate_daily` (not just `day_count`), so a runner-opened day-rate WO renders as **Day** with a flat total instead of multiplying the day rate by hours. `WOFormSync.rate_type` tightened to the `'hourly' | 'daily'` union (the DB `bookings.rate_type` uses `'day'|'hour'`; the two are bridged by save/load conversions). Calendar `DayView` cards stopped rendering `b.notes` to match the desktop grid block's info set (mobile-default Day view was showing notes the desktop grid never did). |
| **WO creation centralized ✅** | **One canonical creator at booking-save; adopt-only elsewhere; `UNIQUE(booking_id)` constraint** |
| wo-creation-centralized | **(`e2da7e8`)** New `lib/createWorkOrder.ts` — `createWorkOrderForBooking(booking)` is the single code path that inserts a `work_orders` row (+ `studio_time_rows` + `equipment_condition_rows`), idempotent `upsert(onConflict:'booking_id', ignoreDuplicates)` (seeds only on fresh insert); `bookingShouldHaveWorkOrder` gate (session types get WOs; Tech/Tour/Open Hours/cancelled excluded by `status`). Called once at booking-save (`calendar handleSave`, new bookings, non-blocking `woWarning` banner; the edit branch keeps FIX 2). `WorkOrderPopup.initWO` adopt-first + create fallback (own create block deleted); runner WO page strictly adopt-only (`woMissing` + null `woData`/`bkData` guards); runner hub `woMap` earliest-wins. DB `UNIQUE (booking_id)` on `work_orders` (manual — the `onConflict` upsert requires it; coupled deploy). FIX 1 folded in / FIX 2 retained (edit-branch) / FIX 3 upserts deleted. Ends the check-then-insert duplicate race (was 27/44 booked WOs duplicated) and collapses the five divergent day-rate seed paths into one. Preceded by a read-only audit proving no server-side trigger/webhook/cron creates WOs. |
| **Nav / splash / glow polish ✅** | **Nav lockup gap, splash name fade-in, login↔splash icon continuity, softer icon glow** |
| nav-splash-glow-polish | **(`4f65eb3`/`672e43d`)** Nav icon→wordmark `gap 12→4` (tight lockup — the icon's 1.4× glow already fills the old gap). Splash name fade-in retriggered on `profile.display_name` load (still a 300 ms beat) so the existing 0.6 s ease fade animates the *real* name instead of the empty placeholder — the transition was already symmetric with the fade-out; only the trigger changed (refines the June 26 `0050c76` blind-timer that raced the async profile load). Splash name `marginBottom:108` shifts the centered lockup up ~54 px so the icon lands where the login screen's icon sits (no downward jump on login→splash). `PRSFloIcon` glow `radial-gradient` given a midpoint stop + 55 % cutoff (was 70 %) so it disperses fast as ambient light, not a visible circle (applies everywhere the icon renders: nav 38, login/reset/splash 72, runner hub 32). |
| **Public inquiry form ✅** | **Public `/inquiry` lead-capture form; auto-creates a Web Inquiry lead** |
| public-inquiry-form | **(`acd65f3`/`9857f6f`/`8e25593`)** `app/inquiry/page.tsx` — public (no AuthGuard, outside `(main)`) form (first/last/email/phone/notes) that inserts a `leads` row (`status:'uncontacted'`, `source:'Web Inquiry'`) — **originally via the browser anon key; since July 2, 2026 it POSTs to the service-role `/api/inquiry` route (3/min per IP) and the anon insert path was removed**; in-place thank-you on submit (no redirect). Full-bleed studio photo (`public/inquiry-bg.jpg`) + dark gradient + frosted card + `public/paramount-logo.png` (PRSFlo wordmark dropped on this page). |
| **Web Inquiry real-time notifications ✅** | **Pulse + tab badge + toasts driven by one shared `leads` channel** |
| web-inquiry-notifications | **(`7cb7d70`/`fc6d03a`/`a1d7cd6`)** `WebInquiryProvider` (mounted once in `(main)/layout.tsx` inside AuthGuard) owns one `leads` channel and drives: (1) pulse/glow on the dashboard Needs Action card, (2) browser-tab title badge `(N New) PRSFlo`, (3) site-wide slide-in toasts (`WebInquiryToaster`). Unacked = `source='Web Inquiry'` + `status='uncontacted'`; hydrates on mount (overnight inquiries survive refresh), clears on a status-change UPDATE (never on click). `leadsVersion` counter = shared re-fetch signal (dashboard Needs Action + CRM watch it, no per-view `leads` channels). `supabase/leads-realtime.sql` (leads → publication + `REPLICA IDENTITY FULL`); `webInquiryPulse` keyframe added to `globals.css`; toast z-index raised above nav. |
| **Full real-time subscription pass ✅** | **Every remaining fetch paired with a subscription; codified as a hard rule** |
| full-realtime-pass | **(`f882695`/`5c30be7`)** Added `postgres_changes` subscriptions (paired with the fetch, unique channel names, `removeChannel` cleanup) to clients, CRM, dashboard, runner checklist/mics/petty-cash, `DailyOpsLogSection`, `FlagsLogSection`, `MicInventorySection`, `DailyOpsModal`, `LocationStrip`, and Nav. `supabase/realtime-publication.sql` batch-adds 18 tables to the publication + `REPLICA IDENTITY FULL` (run once, manual, before deploy). Codified as a Standing Architecture Rule at the top of CLAUDE.md (real-time data is a hard requirement; `WebInquiryProvider` is the reference pattern). |
| **Per-studio color removal + dashboard polish ✅** | **Unify all surfaces onto design-system tokens; dashboard header/shortcut polish** |
| color-removal-dashboard-polish | **(`aeda786`/`c3f176d`/`8dee19d`)** Comprehensive removal of per-studio color coding across every runner page + admin sections + LocationStrip + dashboard, unified onto the design tokens. Dashboard: header actions greyed, Hot/Warm colors corrected, task-tab tweaks, new-lead + flags shortcuts, room-grid name legibility; minor `SectionHeader`/`StatusBadge` adjustments. |
| **CRM tab logic + avatars + auto-initials ✅** | **Status-only buckets, temperature initials avatars, profile-driven initials** |
| crm-tabs-avatars-initials | **(`4afe1e8`/`8564aa4`/`3c4113e`)** All-Leads Uncontacted bucket = `status==='uncontacted'` only (never-contacted hot/warm no longer double-listed; Hot/Warm exclude uncontacted; Needs Action timer-gated tabs untouched). Lead cards get a temperature initials avatar (36px circle, transparent + 2px ring + matching text, `LEAD_AVATAR_COLORS` one color per status; uncontacted `#7BA7BC` = the tab/pill blue) replacing the left/right bookend bars; selected-row highlight neutral `rgba(255,255,255,0.04)`. CRM contact prompts (Touch/Keep Hot/Keep Warm/Dead) + admin Mic Inventory editing auto-fill initials read-only from profile; runner mics/checklist stay manual (public, initials-gated). |
| **`user_profiles.initials` column ✅** | **Stored staff initials, source of truth for auto-fill** |
| user-profiles-initials | **(`a7bb9c5`)** `supabase/user_profiles-initials.sql` adds nullable `initials text`, seeded by `display_name ILIKE` (ES/FR/AA/AM/IH/QC/TD/SS). `UserProfile.initials?: string \| null`; `useUserProfile` already `select('*')`. Read `profile?.initials || profileInitials(profile?.display_name)` everywhere initials auto-populate. |
| **CRM multi-select status tabs ✅** | **Independent toggles, default active pipeline, "All" removed** |
| crm-multiselect-tabs | **(`f410031`)** All-Leads status tabs → independent toggles backed by `Set<StatusFilter>` (default `{uncontacted,hot,warm}`); "All" tab removed; empty set → show every status; per-status badge counts; persisted to `sessionStorage` (`crm_al_active`). `leadStatusKey()` maps cold/dead → the `cold-dead` bucket. |
| **CRM lead-profile redesign (v2) ✅** | **Contrast zones, no section headers, neutral heat buttons, compact session, activity below notes** |
| crm-leadprofile-redesign-2 | **(`8ebae5b`/`9d8d797`/`c5fde75`)** Removed `FieldGroupLabel` section headers; Keep Hot/Warm list-row buttons neutralized (transparent/`var(--border)`/`var(--text)`, status pill unchanged). Two edge-to-edge contrast zones via `margin:0 -16px`+`padding:12px 16px` breakout + single `var(--border)` divider: Zone 1 (identity+contact) `#161920`, Zone 2 (session grid + Engineer Needed) `#1c1f27` (initial values were swapped, corrected in `c5fde75`). Compact session: studio `maxWidth:200`, rate input inline with the /hr /day toggle, Start–End a tight fixed-width pair. Reordered Engineer → Notes → Activity. |
| **Login stagger + crossfade splash ✅** | **Staggered login entrance; 400ms fade-out → splash fade-in; splash cleanup** |
| login-stagger-crossfade | **(`e527f07`)** `fadeUp` keyframe (opacity + translateY, injected via inline `<style>`) staggers icon (0.1s) → wordmark (0.25s) → PRG line (0.38s) → email (0.52s)/password (0.64s)/Sign In (0.76s)/forgot (0.88s) via `fadeUpStyle(delay,dur)` with `forwards`; PRG line moved directly under the wordmark. On successful auth the page fades out (`opacity 0`, 400ms) then `router.replace('/')`; splash `welcomeFadeIn` bumped 0.3s→0.4s to match (both bg `#0d0f14`, no flash). Splash `PARAMOUNT RECORDING GROUP` footer removed; icon→greeting gap set to 2px to match the login icon→wordmark spacing. |
| **CRM DNB rename + translucent tint fix ✅** | **`dead` status displays as "DNB"; Cold and DNB split into separate filter tabs; tint rendering fix** |
| crm-dnb-rename | **(`db31756`/`5bb800d`)** Display-only relabel of `'dead'` lead status → **"DNB" (Did Not Book)** in all UI (pills, tabs, contact prompts, activity log). No DB/schema change. All-Leads tabs split from Cold+Dead into Cold and DNB separately. Translucent bg/border values converted from invalid `${var}22` hex-alpha syntax to `color-mix(in srgb, <color> N%, transparent)` across lead status pills, filter tabs, and the temperature selector. |
| **CRM Analytics date-range filter ✅** | **AnalyticsView date-range select is now live with preset and custom ranges** |
| crm-analytics-date-filter | **(`3d553fe`)** `getAnalyticsRange()` + presets (All Time / This+Last Month / This+Last Quarter / This+Last Year / Custom). AnalyticsView derives `leadsInRange` by `created_at` and drives all stat tiles + six charts off it. Custom Range reveals two `<input type="date">` fields. Stat sub-labels and chart subtitles reflect the active range. |
| **CRM COD polish ✅** | **Artist name field, reg panel UX fixes, contact button neutrals** |
| crm-cod-polish | **(`b4ea627`/`0bd7781`/`4f3b8b2`)** COD new-lead form: "Artist Name" input added. COD lead detail: artist name displayed below the hero name. Reg panel: "Reg Sent" state only appears after a copy/email action; reg link is a teal hyperlink; panel auto-closes after action. Contact (Call/Text/Email) buttons: removed status-color bleeding — always render as the outlined-dark neutral pill. |
| **Tags on leads and clients ✅** | **`tags text[]` on both tables; STARTER_TAGS quick-chips; tag UI in lead detail, new lead modal, client profile; sync on conversion** |
| crm-tags | **(`f5886d1`/`e2169d4`)** Migration `20260720130000_tags.sql`: `tags text[] NOT NULL DEFAULT '{}'` added to `leads` and `clients`. New `lib/tags.ts` with `STARTER_TAGS` constant. Tag UI: applied chips (with `×` remove) + starter quick-chips + custom Enter-to-add input; wired to immediate `supabase.update()` in lead detail, included in `handleSave` payload in new-lead modal, wired to client profile save. Tags carry forward from lead to client on the Confirm Client Account conversion flow. `Lead.tags` + `Client.tags` added to `lib/supabase.ts`. |
| **Email campaigns ✅** | **First-party email marketing in CRM: segment → preview → compose → send via Resend; `email_campaigns` history table** |
| email-campaigns | **(`a4e2070`/`1f97023`/`25e19c1`)** Migration `20260720140000_email_campaigns.sql`: `email_opt_out boolean` on `leads` + `clients`; `email_campaigns` table (subject/body/segment snapshot/recipient_count/sent_by/results jsonb); owner-only RLS. New CAMPAIGNS tab in CRM (gated to Eli's email addresses). `CampaignsPanel`: status multi-select (with DNB), billing filter, tag filter → recipient count + expandable list (excludes `email_opt_out` + missing email) → subject + body compose (with `[First Name]` personalization) → Send → history. `app/api/send-campaign/route.ts`: Resend REST API, `[First Name]` substitution per recipient, service-role `email_campaigns` insert; returns `503` when `RESEND_API_KEY` absent. `CampaignResult`+`EmailCampaign` types in `lib/supabase.ts`. **Status:** domain `paramountrecording.com` added in Resend; DNS records pending propagation; add `RESEND_API_KEY` to Vercel once verified. |

| **v1.4.0 – v1.4.3 — July 29, 2026 ✅** | **DEV tab (feedback + PIN-gated test batches) · runner error checks · handoff docs** |
| dev-tab-and-testing | Nav `Feedback` → **`DEV`** (route stays `/feedback`), rebuilt with a left sidebar mirroring Admin: Feedback (any staff) · Testing (PIN `4321`) · Errors (**Eli's accounts only** — hides the surface; `app_errors` RLS still allows owner/manager, which the export depends on). **Test batch definitions live in code** (`lib/testBatches.ts`), only verdicts in the DB (migration `20260729120000_test_results.sql`, unique on `(batch_id, item_id)`) — a batch ships in the same commit as the work it covers, and **item ids are half the results key so must never be renamed after testing starts**. Batch status is *derived* from results, not stored. **Floating tester panel** in the `(main)` layout — one item at a time, draggable with clamped position, notes above the verdict, Prev/Next with Next gated on a verdict; deliberately NOT on `/runner` (runner testing is phone-in-hand with the checklist on a computer) and items carry a Phone/Desktop badge. **Runner WO error checks:** `handleSaveChanges` used to `router.push` back to the hub unconditionally with no writes checked — a failed save on studio wifi bounced the runner away believing it saved; it now reports and stays put. **Failure export:** "Copy for Claude" (Errors) + "Copy failures + notes" (batch review), since Claude has no DB access. Third-party noise filtered from the error sink. **Fixes:** duplicate realtime channel names in `useTestResults` (three simultaneous mounts → supabase-js returns the same subscribed channel → `.on()` after `subscribe()` throws — this was the live error-boundary entry); panel resuming at item 1 (positioning ran before the results fetch resolved); note-save defaulting `status` to `'fail'`. **Docs:** new `docs/CHANGELOG.md` (per-version, with migrations + watch-outs) and `docs/ONBOARDING.md` (cold-start: architecture, setup, landmines, operational access map). SOP version history gained collapsible dev notes that **mirror** the changelog. |
| **v1.3.2 — July 29, 2026 ✅** | **Bulk editing moved to Admin as an explicit Batch Edit panel** |
| admin-batch-edit | Supersedes v1.3.0/v1.3.1's runner all-days and admin `⇊` fill-down. **Runner is single-row only** (`saveStaffName()` — a runner records their own shift from a shared phone; an accidental all-days rewrites days they can't see). **Admin gains a Batch Edit panel** from the Studio Time `SectionHeader` action: scope *All days* or *Date range*, and per-field checkboxes for room, start, end, rate + Day/Hr type, OT hours, OT rate, staff (1ST/2ND + roster) and session info. **Unticked fields are never written**, so a blank input can't wipe a column. Targets dated STUDIO rows only — standalone staff rows excluded (no room/times/rate of their own) and `admin_locked` excluded, with the footer reporting skipped approved days. Staff **sets** the row's single staffer+role (both roles on a day needs a standalone row — an add, not an edit). Routes through `updateStRow` so all derived billing recalcs through one path; local-first so **Cancel reverts the whole batch**. Removed: `applyFieldToAllDays`, `fillDownTargetCount`, `lastEditedRowId`. |
| **v1.3.0 — July 29, 2026 ✅** | **Runner WO readability · staff reassignment · admin fill-down to all days** |
| runner-wo-readability | *Runner Studio Time table:* type 9–10px→11–12px, numbers off `--text2`, cells centred, **Date + Studio frozen** (`position: sticky` — backgrounds must be OPAQUE, composited as `linear-gradient(tint,tint), var(--surface)`; passing the raw translucent tint let scrolling columns bleed through), **Studio column added** (never existed — the cause of the fake "duplicate rows" report), **zebra by day** not by row, **today-only default + `All N days` toggle** (render-only; `handleSaveChanges` iterates all `stRows` so hidden-day edits still save). *Staff reassignment:* pill popover → **Change** → top-anchored sheet with a role-filtered `<datalist>` from `useStaffPools()` (shared with the CRM lead picker), free text allowed, **"Save this day" / "Save all N {role} days"** (scoped by role so changing the engineer can't wipe assistants); written immediately because `eng_name` is not part of `handleSaveChanges`; `dbResult` imported into the runner WO page for the first time. *Admin WO:* `applyFieldToAllDays(sourceId, keys)` + a `⇊` shown only on `lastEditedRowId` — routed through `updateStRow` so all derived billing values recalc through one path; skips undated, staff-only and `admin_locked` rows; local-first so Cancel reverts. *Consolidation:* **`getLocalToday` → `lib/time.ts`** (was byte-identical in 5 files) and the runner table's `ST_GRID` column template defined once (was 3 copies). **Card layout considered and dropped** — 30-day WOs make cards unusable and runners need every column; see PROJECT_LOG for the precondition if ever revisited. |
| **v1.2.0 — July 28, 2026 ✅** | **Staffing picked on the lead, auto-seeded into every WO studio-time row** |
| staffing-from-lead | Migrations `20260728210000` (`studio_time_rows.eng_role` DEFAULT → `assistant`; default only, no row rewritten) + `20260728220000` (`leads.staff_role`/`staff_name`, `bookings.staff_mode` NOT NULL default `assistant`, both CHECK'd; backfills from `engineer_needed`, which is now **vestigial**). New **`components/shared/StaffPicker.tsx`** — Eng / Asst / **No Staff** + an optional person, shared by the lead detail AND the new-lead modal. Name is **optional** ("engineer, TBD" is valid); the list is the `engineers` roster filtered per pool (`role='Both'` shows in either) but **free text is allowed**. `bookings.staff_mode` is the single signal `createWorkOrderForBooking` reads — an explicit column, NOT `engineer_status` overloading, which can't express "no staff" (it already defaults to `not_needed`). `'none'` seeds rows with the staff sub-row hidden. **Two bugs fixed en route:** `buildRowPayload` wrote `eng_role` only inside its `if (rate or name)` branch, silently dropping the role on the TBD case; and `clearEngRow` reset a cleared row to *engineer*, silently promoting 2ND → 1ST. Every default flipped in step across `seedStudioTimeRows`, `normalizeStRow`, `addStRow`, `addEngRow`, `clearEngRow`, the Seed panel toggle and the `buildBookingProjection` fallbacks. Light-mode `[data-eng-needed]` → `[data-staff-mode-on]`. |
| **v1.1.1 — July 28, 2026 ✅** | **Start Booking restored to the lead detail; lead closes out on session SAVE** |
| start-booking-restored | Reverts the July 8 temporary Confirm-Client swap (`7332eb8`) now that the WO is a real booking destination. Button sits back in the lead hero row; a lead with a `client_id` goes straight to `/calendar?newBooking=1&clientId=…&leadId=…`, one without gets the confirm-client step first, so a session always links to a real client (that link is what drives rename propagation). New `confirmIntent: 'book' \| 'status'` disambiguates the modal, which is shared with the status pill. **The lead is marked booked when the SESSION IS SAVED**, not when the button is pressed — `WorkOrderPopup` takes an optional `leadId` and writes the status after the atomic RPC succeeds (deliberately a separate write: a CRM status change isn't part of the WO's all-or-nothing unit, and must not roll back a saved session). `calendar` holds `woLeadId`, cleared in `openNew`/`openEdit`/`onClose`. **Marking a lead Booked on the status pill stays a distinct act** — a deal often closes before dates settle — so those modals are kept and their stale "TEMPORARY" comments were rewritten. Known limit: the lead link is component state, so an abandoned-then-reopened WO won't mark the lead (safe direction); durable fix is `bookings.lead_id`, carried into Step 9. |
| **CRM batch — July 28, 2026 (session 3) ✅** | **Shared Asst Mgr tasks · registrations database · lead date ranges · client-rename propagation** |
| crm-batch-jul28 | Migrations `20260728190000_leads_session_end_date.sql` (`leads.session_end_date text`, nullable) + `20260728200000_dashboard_tasks_peer_role.sql` (`is_task_peer(uuid)` SECURITY DEFINER helper; `dashboard_tasks` SELECT/UPDATE widened with a peer clause so PAIRED roles — `asst_manager` Quinn+Isaac, `tech` Sierra+Tom — share non-private tasks; `is_private` still guards Eli's self-tasks; DELETE unchanged). `lib/tasks.ts` own-only fetchers dropped their `.or()` filter — it re-implemented the policy client-side and was what hid a teammate's task; RLS is the authority. **Registrations:** new CRM REGISTRATIONS tab (`RegistrationsView`, 25/page + search + date range → `RegViewModal`); pending-reg banner extracted to `RegistrationBanner` and moved to CRM page level. **Copy Address** button + `buildMailingBlock()` in `RegViewModal`. **Leads:** optional End Date in the new-lead modal + lead detail, range rendering in `fmtSessionLine`, calendar lead→session seeds `end_date` from it. **Needs Action:** redundant "Incomplete" tab removed (its leads were already counted in the other three, inflating the header total); `sessionStorage` tab restore now validated. **Web inquiries** pulse in both CRM lead lists (shared `leadRowStyle()`); `webInquiryPulse` keyframe moved off hardcoded lime to `rgba(var(--accent-rgb),…)`. **Label clients** creatable from a company name alone (A&R + contact optional), with duplicate detection extended to the company field. **Renames propagate** via `lib/propagateClientRename.ts`. **Realtime:** new `hooks/useClientsVersion.ts` shared `clients` channel — four consumers, one subscription (was two subscriptions, two consumers). |

### Next

| Priority | What's next |
|---|---|
| **Immediate** | ~~Atomic save RPCs~~ + ~~Step 8~~ — ✅ both shipped July 28 session 2. BookingForm.tsx is deleted; every session/block opens the WO (legacy WO-less blocks get the block editor, and flipping one to a session status promotes it to a real WO). Next: **Step 9** (retire `work_orders.booking_id`) — fully scoped in PROJECT_LOG (7 surfaces must invert to `bookings.work_order_id`, create-RPC idempotency re-keys to a booking row-lock, then the drop migration). Then UI redo Phase 2 (page-by-page, redo = refactor). | ~~Old: resume at Step 5b~~ — 5b/6/7 + blocks shipped July 28. Steps 1–5a shipped (see Done + the July 21 PROJECT_LOG entry + `docs/WO-SPEC.md`). Next: **5b** multi-room card splitting (one booking card per consecutive-same-room segment; upsert on `(work_order_id, studio, start_date)`, mind `bookings` NOT NULL columns) → **6** calendar opens the WO directly (double-click card/empty day → WO, no BookingForm; uses `bookings.work_order_id`) → **7** richer card → **8** delete `BookingForm.tsx` → **9** drop `work_orders.booking_id` → **10** verify all projection consumers. |
| **Immediate — not code** | 🔴 **OPEN: credential single point of failure.** Every login for this system (Supabase, Vercel, GitHub, the domain registrar via Pixelgate, the Google backup service account) exists in **one locked note in the Notes app on Eli's MacBook — a single copy**. If that laptop or Eli is unavailable, the app keeps running but **nobody can deploy, fix, restore a backup, or renew the domain.** `docs/ONBOARDING.md` tells a successor exactly what to do; it cannot give them access. **Fix:** a password manager with an emergency-contact / delayed-access feature (1Password or Bitwarden — nominated person gets access only after an unanswered request window), or at minimum a shared vault with Adam-Mike, plus a printed sealed copy in the studio safe as a belt-and-braces second channel. Do NOT solve it by emailing it to yourself or putting it unencrypted in Drive. **Also check** whether Supabase and Vercel are owned by a personal email rather than an organisation — if personal, recovery depends on that email account too, which is another link in the same chain; moving to org ownership with two admins is a larger but worthwhile job. Minutes of work; higher value than anything else on this list. |
| **Immediate** | **Step 9 carries two extra items (added July 28, session 3):** (1) **`bookings.lead_id`** — durable session→lead link; today it's only `woLeadId` in calendar component state, so an abandoned-then-reopened WO loses it and won't mark the lead booked on save. Add it while Step 9 is already reshaping booking↔WO columns, then have `WorkOrderPopup` read the lead off the booking instead of taking a `leadId` prop. (2) **Typed A&R/Admin names must create real `client_contacts` rows** — `ClientPanel`'s A&R and Admin fields are search boxes; an unmatched typed name lands in `bookings.ordered_by` as a dead string with no record and no first/last. Per the standing convention (a person's name is always two separate fields anywhere it's edited — CLAUDE.md → Data model → `clients`), the CRM is compliant as of v1.1.0 and the WO is the last gap. Artist (a stage name) and the signature "Print Name" lines stay single fields by design. |
| **Immediate** | **Resend domain verification** — DNS records now live on `send.paramountrecording.com` (MX + SPF confirmed via `dig`; DKIM/DMARC at root). Click "Verify DNS Records" in Resend, then generate a `RESEND_API_KEY` and add it to Vercel. Then branded HTML email templates (Paramount logo header, body, unsubscribe footer). Campaigns send from `info@paramountrecording.com`. |
| Medium | **Convert `public/sop.html` into a real React page under `(main)`.** It is standalone HTML in an `<iframe>` with its own `:root` colours and fonts and **zero `data-theme` awareness** — so with the app defaulting to LIGHT mode, staff opening SOP get a black page. It also still uses DM Mono as its body font (the app moved to Inter in July), can't share components or CSS variables, and can't read `docs/CHANGELOG.md` — which is why the version-history dev notes have to be hand-mirrored. ~900 lines with bespoke CSS: a real project, not a tidy-up, and **not to be started mid-test-run**. Cheap interim if the dark page becomes annoying first: pass the theme into the iframe and map its variables. |
| Medium | **Activity log on session form and WO** — per-booking/per-WO feed of field changes, status transitions, runner submissions, admin approvals |
| Medium | **Combine WOs** — merge work orders from **different bookings/studios** into one consolidated master WO via explicit linkage (cross-booking merge, not same-booking — `work_orders` now has `UNIQUE (booking_id)`, so one booking = one WO) |
| Low | **Mobile pass — remainder** — dashboard, CRM, calendar, WO popup, and booking form shipped June 26 (see "Mobile responsiveness pass" in Done). Still to do: the **Admin** page (`/admin`, sidebar tabs) and `/wo-hub` mobile review. |
| Horizon | **TV display** — read-only studio status board for wall-mounted screen; shows today's sessions per room in real time |
| Horizon | **Dashboard activity log** — recent studio activity feed (session starts, WO completions, task completions) |

### Deprioritized

| Chunk | Reason |
|---|---|
| Chunk 5 — Webhooks (Squarespace → leads) | Calendar is higher value; revisit after Chunk 6 ships |

### Future (not yet sequenced)

- **Chunk 8 — Admin settings:** Studio configuration, room definitions, rate management
- **Chunk 9 — Auth + RLS:** Supabase Auth, role-based access (office vs runner), enable RLS across all tables in one migration
- **Chunk 10 — Dashboard:** Unified ops view, session calendar widget, recent registrations
